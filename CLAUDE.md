# 태블릿 상담사용 앱 — 프로젝트 컨텍스트

## 프로젝트 개요

병원 연계 인문 프로그램 기반 감정 치유 서비스 플랫폼의 **태블릿 상담사용 앱**.
Figma에서 hi-fi 디자인이 대부분 완성된 상태이며, 이를 클릭 가능한 프로토타입으로 구현하는 것이 목표.

- **대상 기기**: Samsung Galaxy Tab S11 Ultra, 가로(landscape) 전용
- **논리 해상도**: 1138×712 (추정값, 실기기 실측 미완료)
- **데이터**: mock 데이터만 사용. 백엔드 연동 없음
- **범위 제외**: 환자용 화면, 웹 리뉴얼

## 기술 스택

Vite + Vue 3 (JavaScript) + Tailwind CSS v4 + vue-router + lucide-vue-next

**의도적으로 제외한 것 — 다시 제안하지 말 것:**
- TypeScript (첫 프론트엔드 작업, 타입 디버깅은 순수 추가 부담)
- Pinia (mock 데이터 규모에서 불필요. 필요해지면 `reactive()` 객체 하나로 시작)
- PrimeVue 등 UI 라이브러리 (디자인 시스템이 이미 확정되어 오버라이드 비용이 더 큼)
- Rolldown (이 규모에서 빌드 속도 이득이 체감되지 않음)

## 레이아웃 규칙 (필수)

- 루트는 `h-dvh`. **고정 px 높이 금지** — 주소창 유무, 소프트 키보드로 높이가 변함
- 3분할 셸: 좌우 패널 고정 폭 + `shrink-0`, 중앙 `flex-1`
- 스크롤은 각 패널 내부(`overflow-y-auto`)만. 페이지 전체 스크롤 금지
- 주소창 없는 PWA standalone 모드로 배포 예정
  - 브라우저 뒤로가기·새로고침이 없으므로 앱 내 이탈 경로가 항상 존재해야 함
  - Android 제스처 뒤로가기가 `history.back()`으로 연결됨 → **모달은 history entry로 등록**
  - 미저장 경고는 라우터 가드 + 뒤로가기 이벤트 **양쪽 모두**에 걸어야 함

## 디자인 원칙 위계

충돌 시 위쪽이 우선한다.

**Safety and Trust > Minimalism > Consistency > Motion and Friction > Efficiency > Experience**

## 화면의 두 갈래 (중요)

**화면은 무엇을 다루느냐로 갈린다. 우측 환자 패널의 유무가 여기서 정해진다.**

| 갈래 | 화면 | 좌측 네비 | 우측 환자 패널 |
|---|---|---|---|
| **환자**를 다룬다 | 업무 · 일정 · 환자 분석 | 있다 | **있다** |
| **프로그램/프로세스**를 다룬다 | 효과성 분석 · 저작도구 | 있다 | **없다** |
| **환자가 직접 조작한다** | 감정평가 **설문 수행** | **없다** | **없다** |

- **설문 수행 화면은 셸 전체를 걷는다.** 상담사가 태블릿을 환자에게 건네는
  **유일한 순간**이라, 우측 패널에 다른 환자가 보이면 안 되고 좌측 네비로
  환자가 아무 화면에나 들어갈 수 있으면 안 된다. 우측만 감추면 이탈 경로가
  남아 응답이 소실된다. 미착수 화면이지만 라우트 메타를 설계할 때 미리 셈할 것

- 전체 환자 리스트 · 환자 상세는 환자 화면이지만 패널이 **없다**.
  화면 자체가 패널이 하던 일을 더 넓게 해서 같이 두면 같은 정보가 두 번 나온다
- 라우트 `meta.noPatientPanel`이 셸에서 패널을 걷고 중앙 패딩도 36 → 24로 바꾼다
- **패널의 환자를 탭하면 환자 상세로 간다.** 꾹 누르기는 그대로 배치다
- **`전체 환자` 머리를 누르면 전체 환자 리스트로 간다.** chevron이 이미 '더 있다'고
  말하고 있어 눌리지 않으면 고장으로 읽힌다. 라벨+chevron을 묶어 행 전체가 대상이다
  (chevron만 두면 터치 타깃이 16이라 44 하한에 못 미친다)
- **패널의 정렬 기준은 전체 환자 리스트 화면과 같은 둘이다** —
  `가나다순`(기본) / `최근 진료순` / `진단순`. 같은 명단을 두 자리에서 보는데 기준이 다르면
  같은 '전체 환자'가 두 순서를 갖는다. **명단 원본을 정렬하지 말 것** —
  `allPatients`가 원본 참조라 sort하면 `최근 환자`(앞 3명)까지 바뀐다.
  팝오버는 `Teleport`로 패널 밖에 그린다(패널이 스크롤 상자라 안에 두면 잘린다).
  **진단순의 나열 순서는 `mocks/analysis.js`의 진단 축에서 가져온다** —
  여기 다시 적으면 분석 차트의 막대 순서와 갈라진다. 같은 진단 안에서는 가나다순
- **패널 정렬은 넷이다** — 가나다순(기본) / 최근 진료순 / 진단순 / 단계순.
  진단·단계의 나열 순서는 둘 다 `mocks/analysis.js`의 축에서 가져온다
- **환자 검색은 패널 안에서 거른다.** 리스트 화면으로 보내면 결과를 꾹 눌러
  배치하는 길이 끊긴다 — 그 화면은 패널을 쓰지 않아 작업 맥락까지 사라진다.
  검색 중에는 두 구간을 합쳐 `검색 결과` 하나로 보여준다(배치 모달과 같은 문법).
  **이름·진단·현재 단계**가 대상이다('감정평가'를 치면 그 단계 환자만 남는다).
  정렬은 그대로 적용된다.
  **행 마크업은 세 구간이 한 벌을 공유한다**(`panelSections`) — 구간마다 복제하면
  한쪽만 고쳐진다

## 확정 설계 규칙 (변경 금지)

- **Accent(brand/500) 용법은 3가지로 동결**: 선택 상태 / 현재 위치 마커 / 드롭 대상. 그 외 사용 금지
- **경고색**: 악화 표현 전용. 개선 표현은 중립색
- **파괴적 조작(삭제)은 `text-danger-fg` + `active:bg-danger-bg`.** 악화 표현과 다른 자리다 —
  악화는 '나빠졌다'는 서술이고 이쪽은 '누르면 없어진다'는 예고다.
  **아이콘만 있는 삭제 버튼은 색이 유일한 신호**라 중립색이면 다른 아이콘과 구별되지 않는다.
  **색과 확인 모달을 함께** 갖춘다 — 내용이 없는 빈 줄만 확인 없이 지운다
  - **전 화면에서 같은 색이다** — 아이콘 삭제 · 테두리 삭제 버튼 · 삭제 확인의 확정 ·
    미저장 경고의 `나가기(저장 안 함)`까지. 자리로 세기를 낮추되 색은 낮추지 않는다
  - 채운 삭제 버튼은 `bg-danger-fill` + `text-on-danger`(pressed `danger-fill-pressed`).
    **이 셋은 다크에서 재매핑하지 않는다** — 흰 글자를 얹는 자리라 밝히면 대비가 무너진다
- **표면 분리는 border로**. 그림자 사용 금지 (다크모드 비호환)
- **터치 타깃**: 최소 44px(하드 플로어), 표준 48px. 히트 에어리어와 시각 경계 분리 허용
- **누를 수 있는 것은 누르는 동안 표면이 바뀐다.** 반응이 없으면 눌렀는지 알 수 없다
  - 평소 표면이 무엇이든 **`active:bg-surface-pressed` 한 값으로 눌린다.**
    라이트에서는 어두워지고(#EEF0F3) 다크에서는 밝아지지만(#2D3139),
    어느 쪽이든 card·container·recessed·canvas 넷 모두와 다른 값이라 항상 보인다
  - **선택된 자리는 `active:bg-selected-bg-pressed`**로 눌러 accent를 잃지 않는다.
    이 값은 brand-50 → **brand-100**이다(brand-200에서 내렸다). 두 단계는 폭이 커서
    눌린 것이 아니라 '다른 상태로 바뀐 것'으로 읽혔다. 한 단계 폭(16)은
    일반 pressed(#FFFFFF→#EEF0F3, 17)와 같고 다크(800→700)도 한 단계다
- **선택 상태는 배경만으로 표현하지 않는다.** `selected-bg`는 흰 표면과 차가 작아
  배경만 두면 선택된 게 잘 안 보인다. `border-border-selected`(자리를 차지하면
  안 되는 곳은 `ring-2 ring-inset ring-border-selected`)를 함께 올린다
- **펼쳐지는 목록은 머리와 본문이 한 상자가 된다.** 둘 다 온전한 라운드 상자로 두면
  맞닿는 자리에 선이 두 줄 겹치고 모서리가 넷 생겨, 안 중요한 경계가 제일 눈에 띈다.
  머리는 `rounded-t-lg border-b-0`, 본문은 `rounded-b-lg border-t-0`으로 걷고
  **둘 사이는 배경색 차이만으로 나눈다.** 테두리 색도 하나로 통일한다
  (프로세스 히스토리가 그 예다)
  - **드래그 중에는 넣지 않는다.** 드롭 상태(active·blocked) 표현과 겹친다
  - 아젠다는 **행과 블록에 함께** 건다. 눌린 동안 행 전체가 눌린 것으로 보인다
  - **지난 시간은 행과 블록이 함께 `active:bg-surface-pressed-strong`으로 눌린다.**
    블록의 평소가 canvas인데 라이트에서 canvas와 pressed가 `#EEF0F3`로 값이 같아
    pressed로는 아무 변화가 없다. 반 단계 더 내려(`neutral-150` = `#E8EAEE`)
    누른 것이 보이게 한다. 다크에서는 `neutral-850`이 그 자리다.
    **행도 같이 내린다** — 블록만 내리면 행(`#EEF0F3`)보다 블록(`#E8EAEE`)이
    어두워 '박스가 눌린 것'으로 읽히는데, 지나지 않은 행은 둘이 같은 색이라
    '행 전체가 눌린 것'으로 읽힌다. 같은 제스처가 두 가지로 읽히면 안 된다.
    `HomeView.vue`의 `pressClass` 하나가 행·블록 양쪽을 정한다
  - **채워진 버튼은 `active:bg-surface-inverse-pressed`.** 확정 버튼이 여기 해당한다
  - 텍스트만 있는 버튼은 배경 대신 `active:text-text-primary`로 눌린다
  - 카드 위의 요소도 `surface-pressed`를 쓴다. `surface-card-pressed`는 카드와
    한 단계밖에 차이가 없어 눌린 것이 거의 보이지 않는다
- **네비게이션 문법**: 탭 = 탐색(상태 불변), 버튼 = 결정(상태 변경)
- **오버레이**: 무거운 콘텐츠 → 대형 모달 + 뒤로가기 전용 dismiss / 가벼운 콘텐츠 → 팝오버 + 외부 탭 dismiss
- **스테퍼 노드 글리프는 상태만 표시**: 완료=체크, 진행중=accent fill, 대기=빈 점선 원. **순서 숫자 사용 금지**
  - **노드는 모두 누를 수 있다.** 완료·진행 중은 그 단계로 가는 진입점이고,
    대기만 못 간다(도달하지 않은 단계에 임의 진입 금지 — 배치 단계 결정과 같은 이유).
    **대기를 누르면 비활성 사유 콜아웃**이 뜬다. 노드 전체가 터치 대상이다
  - **시작 전 환자는 0단계(프로세스 시작)가 '진행 중'이다.** 완료로 칠해도 안 되지만
    전부 대기로 두어도 안 된다 — 그러면 프로세스를 시작할 길 자체가 없어진다
  - **판정은 `mocks/process.js`의 `stepStateOf` 하나다.** 환자 상세의 큰 스테퍼와
    코어 프로세스의 컴팩트 스테퍼가 같은 함수를 부른다. 복제해 뒀더니 위 결함이
    양쪽에 똑같이 생겼다
  - **환자 상세에는 프로세스 진입 버튼이 있다**: 시작 전 → `프로세스 시작`(0단계),
    진행 중 → `이어하기`(현재 단계), 중단 → `프로세스 재시작`(0단계).
    **중단은 끊긴 단계에서 이어붙이지 않는다** — 중단 시점의 데이터가 어디까지
    유효한지 알 수 없어 처음부터 다시 건다. 완료만 갈 곳이 없어 버튼을 두지 않는다.
    화면의 주 행동이라 `bg-surface-inverse` 채움 버튼이다.
    Figma 근거가 없는 초안이다
- **프로세스 화면**: 우측 하단 고정 Primary CTA. 전제조건 미충족 시 비활성
- **오토세이브 없음**. 저장은 명시적 조작으로만. 미저장 이탈 시 3버튼 경고 모달
  - **`나가기(저장 안 함)`은 초안을 되돌린 뒤 이동한다.** 되돌리지 않으면 `isDirty`가
    그대로라 라우터 가드가 다시 막고, 경고만 되풀이돼 버튼이 죽은 것처럼 보인다
- **화면 내부 카드**: 라운드 8, `border-border-default`, `bg-surface-card`, 패딩 좌우 12·상하 8
  (셸 패널의 라운드 16과 구분된다)
- **일정 이벤트 블록**: `bg-surface-container`, 라운드 8, `min-h-44`, 우측 패딩 12
  - 좌측 바는 폭 8로 **항상 렌더**한다. 미표시일 때도 `invisible`로 자리를 유지해 텍스트 정렬을 지킬 것
  - 바 색: 지난 일정 `border-border-default` / 예정·진행 중 `border-border-strong`
  - **타임라인에 선택 상태 없음.** accent는 '진행 중' 배지와 드롭 대상에만 쓴다.
    단 여러 건이 접힌 행의 팝오버가 열려 있는 동안은 예외다 (아래 참고)
- **드롭 상태** (환자를 꾹 눌러 배치하는 동안):
  - 놓을 수 없음 = 행 `opacity-60` + 블록 `bg-danger-bg` + `border-text-disabled`(실선).
    일정이 이미 있거나 지난 시간이면 전부 여기 해당한다
  - **놓을 수 없는 자리에 놓으면 사유 콜아웃이 손 뗀 자리에 뜬다.** 드래그 중에는
    blocked가 보이지만 손을 떼는 순간 그 표현이 통째로 사라져 아무 일도 없는 것처럼 보인다.
    막힌 자리도 `data-blocked-title`/`data-blocked-detail`을 달아야 한다 —
    드롭 대상만 표시하면 '막힌 자리'와 '빈 화면'을 구분 못 해 빗나간 드롭에도 경고가 뜬다.
    `dragState.rejected` → `App.vue`가 띄운다(드래그는 셸이 관리하므로 한 곳에만)
  - 놓을 수 있음 = 블록 `bg-selected-bg` + `border-border-selected` **점선** + accent 바, 시간 라벨 primary
    - **환자는 빈 행에만** 놓을 수 있다. 그 시간에 그 환자를 만나는 일이라 겹칠 수 없다
    - **작업은 다른 작업 위에 겹칠 수 있다.** 환자 일정이 있는 행만 막힌다
    - 원래는 "일정이 있는 행은 드롭 대상이 될 수 없다"였다
      (Figma `time row(filled)`에 drop-active 변형이 없다는 것이 근거였다).
      완료된 작업이 그 시간을 영구히 점유하는 문제 때문에 작업에 한해 풀었다.
      **채워진 행의 drop-active 표현은 Figma 근거가 없는 초안이다**
  - 손가락이 특정 슬롯 위에 있으면 **그 슬롯만 남기고 나머지 후보를 `opacity-40`으로 낮춘다.**
    아직 어느 슬롯 위도 아니면 후보를 모두 같은 세기로 보여준다
  - 빈 행의 블록도 **항상 렌더**한다(평소엔 투명). `v-if`로 새로 붙이면 전환 없이
    즉시 나타나 일정이 있는 행(색 전환 150ms)보다 빨라 보인다
  - **놓는 즉시 확정하지 않는다.** 확인 모달을 거친다 — 오토세이브 없음 규칙의 연장
  - **미배정 할 일도 같은 문법**으로 배치한다. 할 일은 유형 선택 없이 바로 확정
  - 같은 날 같은 대상이 이미 있으면 **중복 경고**를 거친다. 취소 / 그래도 배치 2버튼
- **행 인터랙션**: 같은 행이 탭과 꾹 누르기를 모두 받는다.
  탭 = 상세 열기, 꾹 누르기 = 배치. 배치로 끝난 제스처의 click은
  `dragState.swallowDragClick`이 캡처 단계에서 한 번 삼킨다
  - 작업 행 탭 → 작업 상세(설명 · 완료 · 삭제)
  - 아젠다 업무 블록 탭 → 작업 상세 / **환자 블록 탭 → 일정 상세 모달**
    - 원래는 '환자 블록 탭 → 환자 화면'이었다. 그러면 탭도 꾹 누르기도 차 있어
      **배치를 되돌릴 자리가 없다.** 작업 블록은 탭이 조작 자리로 가는데 환자
      블록만 화면 밖으로 나가는 것도 어긋난다. 환자 상세로 가는 길은 이 모달의
      행으로 남고, 캘린더 팝오버·리스트·우측 패널까지 진입 경로는 여전히 넷이다
    - 거부: 블록에 pencil·trash 상시 노출(모든 환자 블록이 시끄러워지고 Figma
      근거도 없다) / 캘린더 팝오버에서만(아젠다에서 일하는 동안 되돌릴 길이 없다)
  - 아젠다 빈 행 탭 → 그 시간으로 일정 추가
  - **놓을 수 없는 자리를 눌렀을 때는 이유를 말한다.** 아무 반응이 없으면 고장으로 읽힌다.
    지난 시간(아젠다) · 지난 날짜(캘린더)를 누르면 **비활성 사유 콜아웃**이
    팝오버 자리에 뜬다 — `InlineCallout.vue` (Figma `176:5336`)
    - **두 줄이다.** 첫 줄 = 무엇이 안 되는지, 둘째 줄 = **언제 되는지**.
      둘째 줄이 없으면 막힌 건 알아도 뚫을 방법을 모른다
    - **첫 줄만 경고색이고 둘째 줄은 중립색.** 둘 다 경고색이면 덩어리 전체가
      경고로 읽혀 정작 해법인 둘째 줄이 묻힌다
    - 색은 `indicator-warning`(orange)이다. `warning-fg`(amber)가 아니다 —
      Figma가 orange를 쓰고 기한 지난 작업의 경고색과도 같아야 한다
    - 라운드 8 · `px-3 py-2` · 아이콘 12. **문구 길이에 맞춰 줄어든다**
      (같은 자리의 목록 팝오버만 280 고정)
  - 캘린더 팝오버의 업무 행 탭 → 작업 상세. 팝오버를 먼저 닫아
    모달만 history 엔트리를 갖게 한다
- **모달은 하나의 셸(`ModalShell.vue`)을 공유한다**: 배치 확인 · 일정 추가 ·
  작업 추가 · 작업 상세. 라운드 16, 폭 360, `border-border-default`, 스크림은 `--scrim`.
  **history 처리(라우터 경유)도 셸에 있다** — 모달마다 다시 짜면 한쪽만 틀어진다
  - **짧은 확인은 `variant="alert"`**: 폭 402 · 라운드 8 · `px-3 py-2`, 스크롤도
    구분선도 없이 한 덩어리다(Figma `186:6839` · `190:9039`). 버튼 배치가 경고마다
    달라 셸은 상자만 잡고 안쪽은 슬롯에 맡긴다. **스크림 탭으로 닫지 않는다** —
    되돌릴 수 없거나 데이터가 걸린 갈림길이라 선택지를 명시적으로 골라야 한다
  - **모달을 부모가 `v-if`로 그냥 걷으면 안 된다.** 셸이 올려둔 history 엔트리가 남아
    다음 뒤로가기를 소리 없이 삼킨다. 어느 버튼이든 셸의 `dismiss`를 거쳐 닫고,
    선택은 기억해 뒀다가 `@close`에서 부모에게 올린다
    (`UnsavedWarningModal`·`NoteDeleteModal`의 `pick`/`settle`)
  - 확정 버튼 `bg-surface-inverse`, 취소 버튼 `border-border-default` (둘 다 시각 36 / 터치 44)
  - 시간 선택지는 **드롭 가능 규칙과 동일**하다 (비어 있고 지나지 않은 시간)
  - **날짜가 정해지지 않은 경로에서는 날짜를 먼저 고른다.** 캘린더 상단 '일정 추가'가
    그렇다. 날짜를 바꾸면 그 날의 빈 시간으로 목록이 갱신되고 시간 선택은 초기화된다.
    아젠다·팝오버에서 열면 날짜가 이미 정해져 있어 날짜 선택이 나오지 않는다
  - 환자 리스트는 **우측 환자 패널과 같은 구조**다. `최근 환자` 구간 다음에
    `전체 환자` 구간이 이어지고 스크롤로 내려간다. 검색 중에는 두 구간을 합쳐
    `검색 결과` 한 덩어리로 보여준다. 구간 라벨은 `text-count`
  - **'작업 추가'는 날짜·시간을 미정으로 열고**, 둘 다 고르면 그대로 배치된다.
    '일정 추가 → 업무'와 같은 폼이며 기본값만 다르다
  - **중복 경고의 취소는 모달을 닫지 않는다.** 고르던 폼으로 돌아간다(`돌아가기`).
    닫으려면 스크림 탭이나 뒤로가기를 쓴다
  - **업무는 날짜·시간을 '미정'으로 둘 수 있다. 환자 일정은 둘 수 없다** —
    환자를 만나는 일에는 반드시 날짜와 시간이 있다
    - `미정` 칩을 날짜·시간 선택지 **맨 앞**에 두고, 대상이 '업무'일 때만 렌더한다
    - 날짜가 미정이면 고를 시간 자체가 없으므로 시간 구간을 숨긴다
    - 날짜든 시간이든 미정이면 **일정이 아니라 미배정 할 일로 간다.**
      아젠다 타임라인은 시간 키(`HOURS`) 기준이라 시간 없는 이벤트를 그릴 자리가 없다
    - 날짜만 정해진 업무는 그 날짜가 할 일의 `due` 자리에 남는다
    - 날짜가 넘어온 경로(아젠다·팝오버)에서도 **업무일 때는 날짜 구간을 계속 보여준다.**
      미정으로 되돌릴 길이 없으면 안 되기 때문이다. 환자일 때는 지금처럼 숨긴다
  - 확인 모달과 드래그 고스트는 **Figma 화면이 없는 초안**이다. 디자인 확정 시 교체할 것
  - 확인·확정 버튼은 `bg-surface-inverse` + `text-text-inverse`.
    라이트에서 검정, 다크에서 흰색으로 자동 반전된다 (`interactive-primary-fill` 아님)
- **지난 행은 행 전체를 어둡게 깐다.** 지금 시각의 경계를 드러내는 것이 목적이다
  - **opacity가 아니라 배경 토큰을 쓴다.** opacity는 글자까지 같이 씻어내서
    `opacity-40`은 제목 4.44 · 메타 2.99(AA 미달), `opacity-80`도 4.68까지밖에 못 올린다.
    배경만 바꾸면 글자가 온전한 토큰 대비를 유지한다 — 실측 제목 7.21 · 메타 7.21 · 시간 7.75
  - **표면 계단**: 평소 행 `surface-card` / 블록 `surface-container`,
    지난 행 `surface-recessed` / 블록 `surface-canvas`.
    이 순서는 라이트·다크 양쪽에서 같은 방향이다
  - `surface-recessed`는 container보다 **반 단계** 아래다
    (`neutral-75` = 50↔100의 중간 / 다크 `neutral-925` = 900↔950의 중간).
    한 단계를 통째로 내리면 블록과의 간격이 사라져 띠가 뭉갠다.
    반 단계 값을 새로 만든 것은 다크 카드용 `neutral-850`과 같은 선례를 따른 것이다
  - 제목은 지난 행에서 secondary로 낮춘다
  - 드래그 중에는 이 배경을 적용하지 않는다. blocked 표현과 겹친다
  - **진행 중은 명도가 아니라 accent 배지로 표시**: '진행 중' = `text-interactive-default` + bold
- **한 시간에 작업은 여러 건** 들어갈 수 있다(환자 일정은 한 건뿐이다)
  - 행에는 **대표 한 건만** 보이고 나머지는 `+N`으로 접힌다 — 캘린더 셀과 같은 문법
  - 대표는 **완료되지 않은 것 우선, 그 안에서 마지막으로 추가된 것**
  - 접힌 행을 누르면 **팝오버**로 그 시간의 목록이 열린다.
    가벼운 콘텐츠라 모달이 아니고 history entry를 만들지 않는다. 행 왼쪽에 붙인다
  - **팝오버가 열린 행은 선택 상태다**: `bg-selected-bg` + `ring-2 ring-inset ring-border-selected`.
    캘린더 선택 칸과 같은 문법이고 accent 용법 '선택 상태'에 해당한다.
    목록을 볼 때 어느 행에서 열렸는지 잃지 않기 위한 것이다
    (그래서 "타임라인에 선택 상태 없음" 규칙은 여기서 예외를 갖는다)

## 작업(업무)의 모델 (중요)

**업무는 '미배정 할 일'과 '일정에 놓인 업무'로 나뉘지 않는다.** 하나의 작업이
날짜·시간을 갖거나 갖지 않을 뿐이다. 명칭도 '할 일'이 아니라 **'작업'**으로 통일한다.

- **배치해도 목록에서 사라지지 않는다.** 리마인더로 남는다.
  목록에서 내려가는 유일한 길은 **완료**다
- 그래서 같은 업무가 작업 목록과 타임라인 양쪽에 보인다.
  완료 상태가 두 곳에 생기지 않도록 **타임라인의 업무 블록은 작업에서 파생**한다.
  `mocks/schedule.js`에는 환자 일정만 있고 업무는 `mocks/tasks.js`에 있다
- `date`만 있고 `hour`가 없으면 타임라인에 자리가 없다 — 목록에만 남는다
- **환자 분류 축은 `mocks/analysis.js`의 `dimensions` 하나다.** `현재 단계`(status)
  축을 여기 더했고 `chart: false`라 **분석 화면 차트에는 오르지 않는다** —
  차트 축이 늘면 '강조 구간의 합 = KPI' 관계를 읽을 자리가 늘고 프로세스 상태와
  뜻이 겹친다. 분석 화면은 `chartDimensions`, 리스트·패널은 `dimensions`를 쓴다
- **필터 팝오버는 축 머리를 `surface-field`로 깔고 위아래 구분선을 둔다.**
  축 이름이 항목과 같은 표면에 있으면 어디까지가 한 축인지 읽히지 않는다
- **완료는 명시적 조작**이고 되돌리기도 같은 자리(작업 상세)에서 한다.
  스낵바 실행취소가 아니다
- **삭제도 같은 자리다.** 완료와 다른 일이다 — 완료는 '했다'는 기록이라 완료 구간에
  남지만, 삭제는 애초에 없었던 것으로 만든다. `DeleteConfirmModal`을 거치고,
  버튼은 테두리만 둔 채 왼쪽에 떨어뜨린다(일정 상세와 같은 배치).
  배치된 작업을 지우면 **타임라인 블록도 같이 사라진다** — 블록이 작업에서 파생이다
- 완료 표시는 **체크 + 중립색**. 완료는 개선 표현이므로 경고색을 쓰지 않는다
- 작업 목록은 **아코디언 3구간**이다: `미배정` → `배정됨` → `완료`.
  **완료만 기본으로 접혀 있다.** 배정 여부는 `hour`가 정한다 —
  날짜만 있고 시간이 없으면 타임라인에 자리가 없으므로 미배정이다
- 헤더의 `작업 · N`은 **미완료 총합**이다
- 완료된 작업은 **배치 대상이 아니다.** 꾹 누르기가 걸리지 않고 탭만 받는다

**작업 행의 시점 라벨**: 어제·오늘·내일만 상대 표기로 줄이고 나머지는 `7/31 (금)`.
시간이 있으면 뒤에 붙인다(`오늘 13:00`). 기한이 지난 **미완료** 작업만
`N일 지남` + 경고색이다 — 완료된 것은 지난 날짜여도 악화가 아니다.
`D-3` 형태는 자리는 아끼지만 날짜를 다시 계산해야 읽히고, 지난 건이 `D+1`이 되어
부호가 헷갈려서 쓰지 않는다.

## 일정과 환자의 관계 (중요)

**일정은 환자의 현재 상태를 참조하지 않는다.** 각자 자기 데이터를 갖는다.

- 일정 이벤트: 그때 무엇을 했는지 (`meta`를 자기가 들고 있음)
- 환자: 지금 어디까지 왔는지 (`status`) + 다음에 할 것 (`nextStep`)

그래서 같은 환자라도 27일은 '프로세스 시작', 29일은 '감정평가'일 수 있다.
아젠다와 환자 패널은 1:1 대응이 아니다.

**배치 유형은 `일반 상담` / `치유 프로세스` 둘 중 하나. 단계는 고르지도 표시하지도 않는다.**
상담사와 환자가 **대면하는 순간은 감정평가 · 프로그램 수행 · 일반 상담 셋뿐**이고
프로세스 시작 · 프로그램 처방 · 프로세스 종료는 상담사가 혼자 처리해 약속을 잡을
대상이 아니다. 무엇을 하는 자리인지는 프로세스가 이미 정하므로 단계를 한 줄 더 적어도
새로 알려주는 것이 없다. 원래는 `nextStep`을 읽어 `다음 단계 · 감정평가`로 보여주고
`nextStep`이 없으면 치유 프로세스로 배치하지 못하게 막았으나, 둘 다 걷어냈다
(완료·중단 환자를 만날 수 없던 부작용까지 딸려왔다).
- 일정에 남는 내용은 `진단 · 유형`이다(`PTSD · 일반 상담`)
- **유형 기본값은 환자의 프로세스 상태가 정한다** — `완료`·`중단`이면 일반 상담,
  그 밖에는 치유 프로세스. 끝났거나 끊긴 프로세스 안의 일이 아니기 때문이다
- ⚠️ 유형을 `감정평가`/`프로그램 수행`/`일반 상담` 셋으로 두는 안은 미결이다
- **단계를 걷어낸 자리에는 `현재 진행` 한 줄이 들어간다.** 현재 단계 + **몇 개 중 몇 개인지**
  (`설문 7개 중 4개 작성` · `6회차 중 3회차 진행 중`). 단계 이름만으로는 손도 안 댄 것과
  거의 끝낸 것이 같아 보인다. 판정은 `mocks/progress.js`의 `progressOfPatient` 하나이고
  값은 전부 원본(`surveys.js`·`sessions.js`)에서 읽는다 — 환자 레코드에 적어두면 갈라진다
  - **미배정·중단만 경고색**이다. 완료는 악화가 아니다
  - **프로세스가 없는 환자를 치유 프로세스로 잡으면 배정하러 보낸다.**
    확인을 거쳐 `/process/:id/0`으로 간다 — 프로세스가 붙어야 그 자리에서 할 일이
    정해진다. 잡던 일정은 배정 후 다시 잡는다. 유형을 일반 상담으로 바꾸면 그대로 배치된다.
    중복 경고와 같은 문법(모달을 닫지 않고 버튼 행만 바뀐다, `돌아가기`)
  - **이동은 모달이 닫힌 뒤에 한다**(`onClose`). 셸의 history 엔트리가 걷히기 전에
    push하면 모달 엔트리가 스택에 남는다

## 색상 · 타이포그래피

`src/style.css`의 2레이어 토큰 시스템을 사용한다.

- **Layer 1 Primitives** (`--color-neutral-*`, `--color-brand-*` 등): 화면 코드에서 **직접 사용 금지**
  - Figma 스케일에 없는 값이 필요하면 **구간의 중간값을 새 primitive로** 만든다.
    `neutral-850`(다크 카드) · `neutral-75`/`neutral-925`(지난 시간 띠) ·
    `neutral-150`(지난 시간 블록의 pressed)이 그 예다.
    화면 코드에 임의 값을 적지 않기 위한 것이므로 반드시 Semantic 토큰을 하나 붙일 것
- **Layer 2 Semantic** (`bg-surface-card`, `text-text-primary`, `border-border-subtle` 등): 화면 코드는 이것만 사용
- **다크모드**: `.dark`에서 Semantic만 재매핑. 화면 코드에 `dark:` 유틸리티를 쓰지 말 것
- 임의 값(`text-[14px]`, `bg-[#fff]`) 사용 금지. 기존 토큰으로 흡수하거나 토큰을 추가

타이포 토큰: `text-count`(11) `text-caption`(12) `text-label`(13) `text-body`(15) `text-title-sm`(17) `text-title-lg`(22) `text-nav`(22) `text-kpi`(40)

- **line-height는 전 토큰 `normal`** (폰트 메트릭). Figma가 전부 Auto이므로 동일하게 맞춘 것.
  Pretendard의 normal 비율은 1.176 — 17px→20px으로 Figma 실측값과 일치한다.
- **px로 고정하지 말 것.** 고정하면 폰트 메트릭과 어긋난다
- 화면 코드에 `leading-*`를 쓰지 말 것 (토큰이 이미 지정)

폰트: Pretendard Variable / 아이콘: Lucide outline

**줄바꿈은 어절 단위다** — `style.css`의 body에 `word-break: keep-all`.
기본값이면 '사라집니 / 다.'처럼 낱말 한가운데가 갈라진다. 끊을 자리가 없는
긴 문자열만 `overflow-wrap: break-word`로 강제한다.

**조사는 `src/text.js`의 `josa()`가 고른다.** `(으)로`·`을(를)`처럼 괄호를
화면에 남기지 말 것. 세 화면이 같은 규칙을 쓴다

## 모션 (잠정 — Figma 프로토타입 미정의)

아래 값은 **Figma 근거가 없는 잠정 합의**다. 프로토타입이 나오면 교체할 것.

- easing은 `ease-standard` 하나로 통일 (`cubic-bezier(0.2, 0, 0, 1)`)
- duration은 Tailwind 기본 스케일을 그대로 쓴다:
  `duration-100` 상태 피드백 / `duration-150` 드롭 타깃 · 명도 전환 / `duration-200` 위치 이동
- 꾹 누르기 임계 500ms, 이동 허용 오차 10px — `src/dragState.js`의 상수로 관리
- **화면 코드에 임의 duration을 쓰지 말 것.** 위 3단계로 흡수하거나 여기에 추가

## 셸 확정 치수

외곽 패딩 24 / 패널 간격 24 / 좌측 네비 137 / 우측 패널 274 / 중앙 flex-1
컨테이너 라운드 16 / 네비 항목 높이 48, 라운드 8 / 항목 간격 8, 그룹 간격 16
네비 아이콘 24·간격 8·폰트 17 / 유틸 아이콘 16·간격 12·폰트 15(병원명만 12)

## 설계 문서 (필수)

**`docs/UIUX-설계문서.md`는 이 프로젝트의 산출 문서다. 무언가를 만들거나
설계 결정을 바꿀 때마다 반드시 함께 갱신한다.** 코드만 고치고 문서를 두면
인수인계 시점에 근거가 사라진다.

- 1.1~1.3(목적·범위·용어 정의)은 사용자가 원본 docx에 직접 쓴 내용이다.
  **임의로 고치지 말 것.** 고칠 일이 생기면 먼저 알린다
- 화면 설계 결정은 4장 템플릿(문제 정의 / 위반 원칙 / 변경 전·후 /
  **거부된 대안** / 검증 상태)으로 기록한다. 거부한 안을 빼지 말 것 —
  그게 없으면 후속 작업자가 같은 안을 다시 검토한다
- 전역에 걸리는 규칙은 3장, 특정 화면에만 걸리면 4장이다
- 미확정 사항은 본문에 `> **확인 필요:**` / `> **미해결:**`로 남기고,
  **6.2절 표에도 한 줄 추가**한다. 표가 인수인계의 진입점이다
- 확정되면 6.2절 상태를 `확정`으로 바꾸고 본문 인용구를 걷어낸다
- 구현이 끝나면 5.3절 완료/미구현 목록을 옮긴다
- 갱신했으면 문서 상단 '최종 갱신'과 부록 A 이력에 한 줄 남긴다
- **수치·토큰값은 문서에 적지 않는다.** Figma 단독 관리이며,
  문서는 결정과 근거만 다룬다(1.1절). 대비 실측처럼 근거가 되는 수치는 예외

**최종 제출은 docx다. 편집은 md로만 하고, 사용자가 요청하는 시점에**
**기존 워드 양식(표지 · 참여 현황 · 개정 이력 · 목차 · 하이퍼링크 · 스타일)을**
**그대로 이어받아 변환한다.** 요청 전에 미리 docx를 만들지 말 것.
원본 docx(`[25IR7710-TM-01] …docx`)에서 살릴 것은 **양식뿐**이다 —
표지·개정이력·2~3장 본문은 이전 문서의 잔여 내용이므로 참고하지 말 것.

## 작업 방식

- **조기 컴포넌트화 금지**. 화면을 인라인으로 먼저 작성하고, 두 번째 화면에서 반복이 확인된 것만 추출
- 미확인 값은 추측으로 채우지 말고 **"확인 필요"로 명시**할 것
- 확정된 결정은 재논의하지 않는다
- 과도한 엔지니어링 제안 금지

## 현재 상태

**홈의 '빠른 저작'은 즐겨찾기(`mocks/favorites.js`)에서 온다.** 고정 목록이 아니다 —
`mocks/home.js`의 `quickAuthoringItems`는 걷었다.

**환자는 `src/mocks/patients.js` 40명 명단 한 곳에서만 온다.** 우측 패널의
`recentPatients`(앞 3명)·`allPatients`도 여기서 파생하고, 분석 화면도 이 명단을 센다.

**일정 블록에는 `진단 · 그 자리에서 하는 일`이 남는다** — `감정평가 (사후)` ·
`프로그램 수행 (6/10)` · `일반 상담`. 대면하는 순간 셋뿐이라 나오는 값도 셋이다.
**회차는 배치하는 순간의 값을 일정이 적어둔 것**이고(`visitDetail`) 그릴 때 환자에서
읽지 않는다 — 그러면 지난 일정의 회차까지 오늘 값으로 바뀐다.
아직 대면 내용이 없는 단계(시작·처방·종료)에서는 `치유 프로세스`가 그대로 남는다.
목업의 회차 번호는 회차 날짜 규칙이 매기는 것과 같게 맞춰 두었다.

**환자 일정은 `src/mocks/schedule.js`, 업무는 `src/mocks/tasks.js` 한 곳에서만 온다.**
날짜 키(`YYYY-MM-DD`)로 관리하고 아젠다(홈)와 캘린더(일정)가 같은 데이터를 본다.
화면별 목업을 따로 두지 말 것.

**작업 목록 헤더**(폭 221)는 `작업 · N` + 정렬(44) + `작업 추가`(72)가 들어가고
52가 남는다. 정렬은 기준을 고르는 **팝오버**다. 아이콘만으로는 현재 기준을 알 수 없어
토글로 두지 않았다. 기준은 **`시간 빠른 순`(기본) / `추가한 순`** 두 가지이고,
시간 빠른 순은 날짜 다음에 시각까지 본다. 날짜 미정은 맨 뒤로 간다.

**일정 화면 월 그리드**: 6주 × 7일 42칸 고정. 주 시작은 월요일.
**요일 머리부터 격자까지 한 판을 `bg-border-subtle`로 채우고, 날짜 칸만
`bg-surface-container`로 덮는다.** 요일 머리 행과 1px 간격에서는 subtle이 그대로 보인다.
셀에 border를 그리지 말 것. 라운드도 없다(모서리에서 판이 비쳐 외곽선처럼 보인다).
요일 머리는 `text-count` + `text-text-secondary` (opacity 없음).
subtle 배경 위라 opacity를 씌우면 글자가 배경에 씻겨 읽히지 않는다.
선택된 칸은 `bg-selected-bg` + **`ring-2 ring-inset ring-border-selected`**.
border로 주면 자리를 차지해 선택 순간 텍스트가 밀린다.
셀당 일정은 **두 건까지만** 보이고 나머지는 `+N`으로 접힌다.
앞뒤 달 칸(`emphasis=dimmed`)은 **배경을 바꾸지 않는다.** 칸 전체에 `opacity-40`을 주고
글자만 `text-text-secondary`로 낮춘다.
**단, 드래그 중에는 이 `opacity-40`을 걷어낸다.** 드롭 배경까지 40%로 씻겨
8월 칸에 놓으려 할 때 어디가 대상인지 보이지 않기 때문이다.
**누르는 동안에도 같은 이유로 걷어낸다** (`active:opacity-100`).
요일 머리는 `text-count`(11) + `text-text-secondary`.

**캘린더 드롭 단위는 날짜다.** 지난 날이거나 빈 시간이 하나도 없으면 놓을 수 없다.
시간은 확인 모달에서 고르며, 그 날의 **빈 시간만** 선택지로 나온다.
드롭으로 열린 모달은 시간 기본값을 채우지 않는다 — 고르지 않은 시간에 확정되면 안 된다.

**날짜 팝오버**: 일정이 있는 칸을 탭하면 그날 목록이 열린다. 폭 280 / 최대 높이 364.
가벼운 콘텐츠이므로 **모달이 아니라 팝오버** — 외부 탭으로 닫고 history entry를 만들지 않는다.
환자 일정에만 우측 chevron(상세 진입).
칸 오른쪽 8px에 붙이고 세로는 칸 중앙에 맞추되 화면 밖으로 나가지 않게 가둔다.
**일정이 없는 칸을 누르면 팝오버 대신 그 날짜로 '일정 추가' 모달이 열린다.**
배치할 수 없는 날(지난 날)은 추가할 것도 없으므로 아무 일도 하지 않는다.

**환자 분석 화면**(Figma `127:9495`): KPI 카드 → 필터 칩 행 → 프로세스 상태·진단 유형
2열 → 연령대·성별 2열. 카드 간격 8, 콘텐츠는 세로 가운데 정렬(`justify-center`).

- **집계는 화면이 명단에서 직접 센다.** `mocks/patients.js`의 40명이 유일한 원본이고
  분석 화면에 별도 집계 목업을 두지 않는다. `mocks/analysis.js`에는 축 정의만 있다
- **각 차트는 자기 축을 뺀 나머지 필터를 적용한 분포를 센다.**
  그래야 **강조된 구간의 합이 언제나 KPI와 같다** — Figma에서 강조 구간이 전부
  128명으로 떨어지던 것(진행 중 = PTSD = 여 = 40대 이상)이 이 관계다
- **차트의 accent는 '선택 상태'다.** accent 용법 3가지 안에 있다
- **막대를 누르면 그 구간이 필터에 들고 난다.** 히트 영역은 **행/칸 전체**다 —
  트랙의 빈 부분까지 포함한다. 가로 행 250×44, 연령 칸 44×145
- **성별은 칩(원)과 막대 둘 다 같은 필터를 건다.** 막대는 좌우가 각각 자기 성별이다.
  한쪽이 0명이면 그 구간의 폭도 0이 되지만 원이 남아 있어 길이 막히지 않는다
- **칩은 축 단위다.** 탭하면 그 축이 통째로 풀린다. 라벨은 고른 순서가 아니라
  축의 구간 순서를 따르고, 연령이 뒤쪽으로 이어지면 `40대 이상`으로 줄인다
- **막대 길이 = 값 / 최대값 × 트랙의 90%**, 최소 8%. Figma 실측이 이 규칙과 1px 안에서 맞는다
  (프로세스 82→72 · 진단 23→20 · 연령 68→89). 4명 막대의 8px가 최소 길이 쪽이다.
  **0명은 최소 길이도 주지 않는다** — 없는 것을 있는 것처럼 그리면 안 된다
- **가로 막대 트랙은 두 카드에서 같은 폭(125)으로 고정한다.** flex로 늘리면
  라벨 폭(50 vs 60)만큼 트랙이 갈라져 카드 사이의 막대를 비교할 수 없다
- 성별만 분포가 아니라 **비율**이다. 둘이 합쳐 트랙을 채운다
- ⚠️ 40명 명단은 임의 표본이라 Figma의 128명·532명과 규모가 다르다.
  실제 값이 확정되면 명단을 갈아끼우면 된다
- ⚠️ Figma 프레임의 중앙 패널은 626인데 확정 셸 치수(외곽 24 · 간격 24 · 좌 137 · 우 274)로는
  631이 나온다. Figma 쪽 오른쪽 여백이 24가 아니라 29다. **확정 치수를 따랐다**

**전체 환자 리스트**(Figma `127:8671` / 빈 상태 `130:2792`): 검색 섹션(검색바 ·
필터 · 정렬 · 칩 행 · 건수) + 리스트 카드(헤더 4열 + 56 행).

- 우측 환자 패널을 쓰지 않는다(위 '화면의 두 갈래' 참고). 본문 폭이 929가 된다
- **KPI를 누르면 그때 걸린 칩이 쿼리로 넘어온다.** `?process=진행 중&condition=PTSD…`.
  그래야 '5명'을 누른 결과가 5명이다. 필터를 바꾸면 주소도 따라가서
  새로고침·뒤로가기가 같은 결과를 낸다
- **헤더와 행은 같은 열 폭·간격을 쓴다**(245 / 197 / 194 / flex-1, gap 10).
  Figma는 헤더만 245/201/193이라 그대로 옮기면 표가 어긋난다 — 행 쪽에 맞췄다
- **마지막 진료 · 다음 일정은 일정에서 뽑는다**(`visitsOf`). 환자 레코드에 적어두면
  배치할 때마다 갱신해야 하고 한쪽만 갱신되면 두 화면이 갈라진다.
  업무 블록은 진료가 아니므로 환자 일정(`bar`)만 본다
- 마지막 진료는 `오늘` / `어제` / `N일 전`. 오늘 지나간 진료가 `0일 전`으로 읽히면 안 된다
- **다음 일정이 없으면 경고색 + dot**. 잡아야 할 것이 남았다는 뜻이라 악화 표현에 해당한다
- **`필터` 버튼은 걸린 필터가 있을 때만 accent다.** Figma는 accent 고정이지만
  그러면 accent 용법 3가지를 벗어난다. '선택 상태'로 해석해 조건부로 바꿨다
- 필터·정렬 팝오버는 **Figma에 열린 상태가 없어 초안**이다

**환자 상세**(Figma `130:3152` · `148:5384`): 환자 정보 → 프로세스 스테퍼 → 탭 카드.
리스트와 마찬가지로 우측 패널을 쓰지 않는다.
진입 경로는 넷이다 — 아젠다 환자 블록 · 캘린더 팝오버 · 리스트 행 · **우측 환자 패널**.

- 상단(정보 + 스테퍼)은 탭과 무관하게 늘 같고 **하단 카드만 탭으로 바뀐다**.
  탭은 `프로세스 히스토리` / `핵심 지표` / `개인 메모` 3개. **개인 메모는 Figma 디자인이 없다**
- **스테퍼는 확정 규칙 그대로**: 완료=체크, 진행 중=accent fill(▶), 대기=빈 점선 원.
  순서 숫자 없음. 노드 폭 91 · 간격 24 · 원 48 → 전체 666
  - 연결선은 지나온 구간 실선(`border-default`), 앞으로 갈 구간 점선(`text-disabled`)
  - **시작 전 환자는 완료된 단계가 하나도 없다.** 첫 단계까지 완료로 칠하면 안 된다
  - 완료·중단 환자는 '진행 중' 단계가 없다
- **프로세스 이름은 진단을 따른다**(`게임과몰입_v1.0`). 목업이 PTSD 한 명분이라
  그대로 쓰면 게임과몰입 환자에게 `PTSD_v1.0`이 붙는다
- **추이 그래프는 눈금과 점이 같은 좌표계를 쓴다**(SVG viewBox).
  절대 좌표로 점을 찍으면 데이터가 바뀔 때 눈금과 어긋난다.
  점과 값 라벨은 SVG 밖에 얹는다 — `preserveAspectRatio="none"`이라 안에 그리면 원이 찌그러진다
- Key data 카드를 누르면 아래 그래프가 그 지표로 바뀐다
- **눈금은 데이터를 가운데 두고 20 폭 5칸으로 화면이 계산한다.** 지표마다 값의 범위가
  달라 하나로 고정하면(우울 55~75) 감정 통제감 38이 눈금 밖으로 나간다.
  이 규칙으로 우울·불안은 Figma와 같은 55~75가 나온다
- **핵심 지표는 환자별로 만든다**(`keyMetricsOf`) — 환자 id와 지표 id를 해시해
  사전값을 정하고 지표의 개선 방향으로 회차마다 움직인다. 점의 개수는 지나온
  회차 수를 따른다. ⚠️ 산출 규칙은 임의이고 실제 근거가 나오면 이 함수만 바꾼다
- ⚠️ 프로세스 단계 구성·이력·평가 지표는 전부 **임시값**이다.
  우울·불안 추이만 Figma 실측이고 나머지 넷과 종료된 프로세스(v0.3·v0.5)의 내용은
  임의로 채웠다. **상세 치유 프로세스 정보가 나오면 `mocks/process.js`를 갈아끼울 것**

**모달 내부 라운드 위계**: 카드 16 → 그 안은 전부 8. 리스트는 컨테이너 하나(8, border)로
묶고 **행은 라운드 없이 구분선으로만** 나눈다. 라운드 8짜리는 전부 1px border를 갖는다.
모달은 `max-h-full`로 화면을 넘기지 않고 본문만 스크롤한다. 버튼은 하단 고정.

완료:
- 환경 셋업, 토큰 이식, 3분할 셸 + 라우터(5화면), 좌측 네비 전체
- 홈(업무) 화면: 빠른 저작 · 작업(정렬 · 완료 아코디언) · 일정 타임라인 7일치 + 날짜 이동
- 작업 상세 모달(설명 · 완료 · 완료 취소 · 삭제), 아젠다 행 탭 인터랙션
- 우측 환자 패널(환자 화면에만. 탭하면 환자 상세로 간다).
  `전체 환자` 머리 → 리스트 진입, 정렬(가나다순 / 최근 진료순), 환자 검색
- 꾹 누르기 → 드롭 상태 → 고스트 → 확인 모달 → 배치. **환자와 작업 모두**,
  **아젠다와 캘린더 모두**에서 동작
- 모달 하나로 통합(배치 확인 · 일정 추가 · 작업 추가) + 중복 경고 + 날짜/시간 선택
- 일정 화면: 2026년 7월 월 그리드 + 날짜 팝오버 + 날짜 드롭 + 빈 칸 탭 → 일정 추가
- 모달 환자 리스트 2구간(최근 환자 / 전체 환자), 업무 날짜·시간 '미정'
- 환자 분석 화면: KPI · 필터 칩(제거 · 초기화) · 프로세스/진단/연령/성별 차트
- 전체 환자 리스트: 검색 · 필터/정렬 · 칩 · 40명 표 · 빈 상태. KPI에서 필터를 물고 온다
- 환자 상세: 정보 · 스테퍼 · 프로세스 히스토리 · 핵심 지표(추이 그래프).
  **아젠다 환자 블록 · 캘린더 팝오버 · 리스트 행이 모두 여기로 연결됐다**

- 개인 메모 탭: 목록 · 검색 · 새 메모 · 편집 · 삭제 확인 · 미저장 이탈 3버튼 경고
- 일정 편집·삭제: 일정 상세 모달 → 편집(날짜·시간·유형) · 삭제 확인.
  아젠다와 캘린더 양쪽, 행 팝오버·날짜 팝오버에서도 같은 자리로 간다
- 좌측 하단 프로필 사진(`src/assets/profile.png` = 중앙대학교병원 **심볼만 잘라낸 정사각**).
  `import.meta.glob`으로 찾아 파일이 없어도 빌드가 안 깨지고, 없으면 이름 첫 글자로 대체된다.
  원본 가로형 로고는 `hospital-logo-full.png`로 남겨 뒀다(글로브가 잡지 않는 이름).
  `object-cover`로 원을 채운다 — 원본을 그대로 넣으면 워드마크 글자가 뭉개진다
- **로그인 화면 · 역할 분기 · 로그아웃** (Figma 없음 — 임의 제작).
  `/login`은 `meta.bare`로 셸을 걷는다 — 로그인 전에 우측 패널의 환자 명단이
  보이면 안 된다. **가드는 라우터 한 곳**(`router.js`의 `beforeEach`)이고,
  환자 세션으로는 상담사 화면이 열리지 않는다(설문 수행만 예외).
  세션은 `src/authState.js`의 **sessionStorage**에 남긴다 — 새로고침은 견디고
  탭을 닫으면 지워진다(함께 쓰는 기기라 다음 사람이 앞사람 계정으로 들어가면 안 된다).
  **인증을 흉내 내지 않는다.** 무엇을 넣어도 통과하고 화면에도 그렇게 적어 둔다.
  **환자는 계정을 만들지 않고 1회용 접속 코드(숫자 6자리)로 들어온다** —
  코드가 곧 신원이라 환자 쪽만 값을 검사한다(`mocks/accessCodes.js`).
  이름+생년월일은 아는 사람의 정보로 남의 기록을 열 수 있어 걷어냈다.
  ⚠️ 코드는 프로토타입이라 소진되지 않고, 화면이 그 사실과 시연용 코드를 밝힌다
- **환자용 첫 화면**(`/patient`, Figma 없음 — 구버전 웹 구성만 참고).
  위 왼쪽 **마음의 변화**(핵심 지표 추이) · 위 오른쪽 **작성할 설문** · 아래 **치유 기록**.
  값은 전부 상담사 화면과 같은 원본에서 읽는다 — 환자용 목업을 따로 두면
  상담 자리에서 두 화면이 어긋난다
  - **할 일이 제일 크다.** 미완료 설문을 위로 정렬하고, 끝낸 설문은 다시 열리지 않는다
  - **판정·진단 용어를 앞세우지 않는다.** 그래프도 개선·악화로 판정하지 않고
    얼마나 달라졌는지만 적는다(지표마다 좋아지는 방향이 다르다)
  - **지표 이름은 둘이다** — 상담사는 `우울 · 불안`, 환자는 `마음의 무게`.
    `keyMetrics`의 `label`/`patientLabel`이 한 배열에 나란히 있다
  - **치유 기록은 프로그램 단위**다(`programRecordsOf`). 프로세스 이름·버전은
    우리 쪽 관리 단위라 환자 화면에 내밀지 않는다.
    ⚠️ 지난 프로그램은 목업에 없어 진단에 맞는 것 중 결정론적으로 고른다
  - **환자 세션은 제출 뒤 완료 알림을 거치지 않는다.** 그 화면은 태블릿이 환자
    손에 있을 때 상담사용으로 튀지 않게 막는 자리다 — 자기 화면으로 돌아가는
    경우엔 막을 것이 없다. 상담사가 건네준 경우에만 남는다
  - **접속 코드는 환자 상세에서 발급한다**(`AccessCodeModal`).
    새로 내면 이전 코드는 무효다 — 회수 못 한 열쇠를 남기지 않는다
  - 회차를 지나지 않아 그릴 선이 없으면 그 자리에 **다음 만남**(일정에서 읽는다)을 둔다
  - 설문 수행 화면의 복귀 버튼은 세션이 정한다 — 환자면 `내 화면으로 돌아가기`
- **알림 팝오버 · 설정 모달** (Figma 없음 — 임의 제작).
  알림은 읽고 지나가는 가벼운 콘텐츠라 팝오버(폭 280 · 최대 높이 364, 캘린더 팝오버와 같은 값)이고
  버튼 오른쪽에 붙는다. 좌측 네비가 화면 끝이라 아래로 펼치면 화면 밖으로 나간다.
  **읽지 않음에 accent를 쓰지 않는다** — 표면·굵기로 가르고 개수 배지는 중립 채움이다.
  설정은 저장·취소가 있는 자리라 모달이고, **사본을 고쳐 저장에서 한 번에 반영**한다.
  테마 토글은 설정에 두지 않는다 — 좌측 하단에 이미 있다.
  **로그아웃도 여기 있고 확인을 거친다**(버튼 행만 바뀐다).
  데이터는 `mocks/notifications.js`·`mocks/settings.js`
- 비활성 사유 콜아웃(`InlineCallout.vue`) — 아젠다·캘린더 공용
- 코어 프로세스 공통 셸 + **0단계 '프로세스 시작'**: 치유 프로세스 리스트
  (진단 칩 · 검색 · 정렬 · 표 · 구버전) → 상세 모달(단계 아코디언) →
  할당 확인 → 할당. 진단 불일치 경고까지
- 코어 프로세스 **1·4단계 '감정평가'**: 사전/사후 칩 · 설문 카드(핵심·보조 배지 ·
  점수 막대 · 컷오프) · 작성하기/작성완료 · 다음 단계 CTA
- 코어 프로세스 **2단계 '프로그램 처방'**: 감정평가 정보 탭(등급 배지 · 감정 지표) ·
  프로그램 선택 탭(검색 · 정렬 · 필터 넷) · 프로그램 상세 ↔ 세션 상세 한 모달
- 코어 프로세스 **3단계 '프로그램 수행'**: 회차 목록(진행률·상태 배지) ·
  회차 상세 · 수행 내역 기록 · 저장 후 다음 회차
- **설문 수행 화면**: 셸을 걷은 환자용 화면. 문항 표 · 진행률 · 임시 저장 ·
  제출 · 뒤로가기 경고 · 완료 알림 화면. Figma 없이 임의 제작

- 코어 프로세스 **5단계 '프로세스 종료'**: 요약 네 칸(프로세스·기간·처방자·완료 회차) ·
  감정평가 사전/사후 비교 카드 · 종료 시각 · 출력

- **효과성 분석**(웹 구버전 이식, `mocks/effectiveness.js`).
  진단 토글 · 전후 효과성 카드(사전/사후 막대 + p값) · HRV/fNIRS 검증 ·
  생체신호 상관표(네 블록 중 하나만 펼침) · 피어슨 히트맵 · 유사도 · T-검정.
  백엔드 분석에 묶인 화면이라 **기능이 아니라 구성만** 옮겼다
  - **강조에 accent를 쓰지 않는다.** 웹은 전부 파랑이지만 accent 용법은 셋으로
    동결돼 있다 — 값의 크기는 **중립 명도 계단**으로, 유의한 p값은 굵기로 올린다
  - **부호를 색으로 말하지 않는다.** 음의 상관을 경고색으로 칠하면 나쁨으로 읽힌다
  - 웹 상단의 상담사 카드는 걷었다(좌측 네비 하단에 이미 있다).
    표는 **표 안에서만 가로 스크롤**한다
  - **척도 구성은 `surveys.js`에서 온다.** 웹은 PHQ-9·BIS-11·K-CAARS를 쓰지만
    그러면 같은 진단의 환자를 두 화면이 다른 척도로 말한다.
    인문 척도 = 그 코드에 `H-`를 붙인 것, 기존 임상 척도 = 접두 없는 원본
  - ⚠️ HRV·fNIRS 요약과 HbO activation 한 블록만 웹 실측이고 나머지는 임의값이다
- **생체신호 측정·등록**(감정평가 화면의 `생체신호` 버튼 → `BioSignalModal`).
  웹 구버전 이식이고 **모습만** 옮겼다 — 첨부 파일은 이름만 남기고 내용을 읽지 않는다.
  - **프로세스가 정한다**(정의의 `bio`). 설문 구성과 같은 자리다.
    버튼은 언제나 보이되 재지 않는 프로세스에서는 누르면 사유 콜아웃이 뜬다
  - **시점을 물려받는다** — 사전·사후 기록이 섞이면 전후 비교가 성립하지 않는다
  - 대형 모달(690×480)이고 **머리와 HRV·fNIRS 전환은 고정**이다
  - **임상 범위를 벗어난 값은 막지 않고 알린다.** 장비가 그렇게 읽었을 수 있다
  - ⚠️ 상담사가 프로세스 개시에서 켜고 끌 수 있어야 하는지는 미결이다

- **저작도구 이름 체계 · 홈 · 진입**(`mocks/authoring.js` · `AuthoringView` · `AuthoringToolView`).
  Figma가 미완성이라 **편집 화면은 만들지 않았다.** 확정한 것은 이름과 들어가는 길 둘이다
  - **접미사를 걷고 만드는 대상을 이름으로 삼는다.** 저작도구 안은 전부 만드는
    화면이라 '저작'·'명세'가 서로를 구분하지 못한다. 대상 이름은 설계문서 1.3절을 따른다
  - 바뀐 이름: `감정평가`→**척도**(프로세스 1·4단계 화면 이름과 겹친다) ·
    `프로세스 명세`→**프로세스** · `문장 명세`→**도서 콘텐츠**(등록 단위가 도서다) ·
    `프로그램 저작`→**프로그램** · `세션활동 저작`→**세션 활동**(접미사만 걷었다).
    `데이터 필드`·`데이터 명세`는 그대로다
  - **네비는 `저작도구` 한 줄이고 일곱 도구는 홈에서 편다.** 좌측 네비가 다섯
    화면짜리라 일곱을 더 넣으면 네비가 화면의 절반이 된다
  - **화면에는 옛 이름도, 미완성 안내도 적지 않는다.** 쓰는 사람에게 필요한 것은
    이 도구가 무엇을 만드는가뿐이다. 근거는 설계문서 4.8절에만 남긴다
  - **일곱 도구가 화면 하나를 공유한다**(`/authoring/:tool`). 편집 구조가 도구마다
    다를 것이 분명한데 미리 일곱 벌을 만들면 나중에 일곱 곳을 고쳐야 한다.
    없는 키로 들어오면 라우터가 홈으로 되돌린다
  - **목록은 각자의 목업에서 읽는다**(`surveys.js`·`processLibrary.js`·`programs.js`).
    저작도구가 자기 목록을 들면 실제로 쓰이는 목록과 갈라진다.
    목업이 없는 도구는 개수를 적지 않고(0은 '지웠다'로 읽힌다) 사유 콜아웃을 띄운다
  - **목록이 없는 도구는 카드 자체를 두지 않는다.** 빈 상자에 안내를 채우면
    그 문구가 화면의 내용이 된다. 세션 활동은 `programs.js`의 세션 이름을 모아
    보여주다가 걷었다 — 이름만 있고 내용이 없는 목록이 정의가 있는 것처럼 읽힌다
  - ⚠️ 이름은 구버전 웹 화면과 1.3절만 보고 정했다. 데이터 구조가 확정되면 재검토
- **척도 저작**(`/authoring/scale/:code`, `ScaleEditorView.vue`). **편집 화면 1호**다 —
  일곱을 한꺼번에 열지 않고 **하나씩 축소 버전으로** 연다
  - **축소 기준은 '앱이 실제로 읽는 필드'다.** 웹이 받는 버전·스키마 버전·영문명·
    작성기관·역채점·서술형·중증도 구간은 읽는 화면이 없어 뺐다 —
    받아도 입력한 사람이 반영됐는지 확인할 방법이 없다
  - **문항수**는 문항 목록이 정하고, **중증도 구간**은 `gradeOf`가 컷오프 하나와
    핵심/보조로만 갈리므로 받지 않는다
  - **설문 코드는 새로 만들 때만 정한다.** 코드가 응답의 키(`환자:시점:코드`)라
    바꾸면 받아둔 응답이 끊긴다
  - **총점은 저장할 때 다시 센다**(문항 수 × 보기 최고점). `saveSurvey` 한 곳이다
  - 오토세이브 없음 그대로 — 저장 버튼 하나, 미저장 이탈은 3버튼 경고,
    저장 못 하는 이유는 사유 콜아웃
  - **`surveys`가 reactive다.** 저작이 이 객체를 고치는데 평범한 객체면 목록도
    감정평가 화면도 그 자리에서 갱신되지 않는다(환자 명단과 같은 이유)
  - ⚠️ Figma 없음. 구버전 웹 저작 화면의 구성만 참고한 초안이다
- **데이터 필드 저작**(`/authoring/field/:group`, `FieldGroupEditorView.vue`). 편집 화면 2호
  - **편집 단위가 필드가 아니라 그룹이다.** 코드명이 그룹 접두 + 순번이라
    필드 하나만 떼어 보면 그 번호가 어디서 온 값인지 보이지 않는다.
    구버전 웹은 필드 낱개를 고친다
  - **코드명은 받지 않는다.** 그룹 코드와 자리 순서가 이미 정하는 값이라
    손으로 적게 하면 순서를 바꾸는 순간 갈라진다. `codeOf`가 그릴 때 만들고,
    그룹 코드가 비어 있으면 순번만 보인다(물음표로 채우면 오류로 읽힌다)
  - **그룹 코드도 만들 때만 정한다.** 이미 붙은 코드명이 전부 여기서 나온다
  - 유형이 `선택`일 때만 선택지 입력이 나온다(쉼표로 구분)
  - **구버전 우측의 JSON 패널은 두지 않았다.** 폼과 같은 내용을 두 번 그린다
  - **생체신호 두 그룹은 `bioSignals.js`에서 온다** — 같은 항목을 두 곳에 적으면
    측정 화면과 저작도구가 다른 항목을 말한다
  - ⚠️ 기본정보(DS) 14개는 웹 실측이고 단위·유형은 임의다.
    감정평가 분류의 그룹은 무엇이 필드가 되는지 확인되지 않아 두지 않았다

- **프로세스 저작**(`/authoring/process/:id`, `ProcessEditorView.vue`). 편집 화면 3호 · MVP
  - **프로세스의 단계 수는 고정이 아니다.** 감정평가 · 프로그램 처방 · 프로그램 수행
    노드를 몇 개든, 어떤 순서로든 이을 수 있다. 표준형(사전→처방→수행→사후)은
    흔한 한 가지일 뿐이다 — 처방과 수행을 번갈아 도는 프로세스도 성립한다
  - 웹의 자유 캔버스 대신 **세로 시퀀스**로 둔다(추가 · 삭제 · 위아래 이동).
    태블릿 터치로 선을 잇는 조작이 무겁고, 지금 필요한 것은 종류와 순서까지다.
    분기가 필요해지면 그때 캔버스를 연다
  - **감정평가 노드만 시점(사전/사후)을 갖는다.** 안 고르면 미연결이고 같은 시점이
    둘이면 저장을 막는다 — 사전·사후를 이름으로 찾는 화면들이 갈 곳을 잃는다
  - **회차는 정의가 적지 않는다.** 이름·개수는 처방된 프로그램이, 날짜와 간격은
    일정이 정한다. `주 1회 · 5주`를 정의가 약속하면 실제 일정과 어긋난다.
    목업 정의의 단계 메타에서도 주기 문구를 걷었다
  - **생체신호는 토글 하나다.** 앱이 읽는 값이 `bio` 하나뿐이라 HRV/fNIRS로 나눠
    받아도 반영될 자리가 없다
  - 증상을 바꾸면 **그 증상에서 고를 수 없는 설문은 선택에서 빠진다**
  - **처방 노드는 고르는 자리가 아니다.** 처방은 그 자리에서 상담사가 한다 —
    정의가 미리 고르면 처방을 대신하는 셈이고 프로그램 목록이 바뀌면 낡는다.
    그 증상에서 처방할 수 있는 프로그램을 **읽기 전용**으로 보여주고
    효과성이 가장 높은 **하나에만** `추천`이 붙는다
  - **처방·수행 노드는 정의에 내용을 남기지 않는다.** 프로세스 상세 모달도
    '등록된 항목이 없습니다' 대신 무엇이 그 자리를 정하는지 적는다
  - `processLibrary`도 reactive다(저작이 고치면 목록·프로세스 시작 화면이 함께 갱신)
  - 노드 수가 다른 정의도 스테퍼가 따라간다 — 단계는 `stepsOf`가, 현재 단계는
    환자의 `stepIndex`가 정한다(설계문서 6.2절 38번 확정)
- **척도의 분류 축은 둘이다** — 증상(공통·게임과몰입·PTSD·동반이환)과
  **종류(인문·임상)**. 종류는 효과성 분석이 이미 쓰던 구분이다(인문 = 임상 코드에 `H-`)

- **데이터 명세 저작**(`/authoring/spec/:id`, `SpecEditorView.vue`). 편집 화면 4호
  - **명세는 필드를 만들지 않고 고른다.** 원본은 데이터 필드고 명세는 **코드명으로
    가리키기만** 한다 — 두 곳에서 고칠 수 있으면 어느 쪽이 원본인지 알 수 없다
  - **명세는 그룹 하나에 붙는다.** 코드명이 그룹 접두 + 순번이라 여러 그룹을 섞으면
    같은 순번이 두 번 나온다. 그룹을 바꾸면 고른 필드를 비운다
  - **분류는 그룹이 정한다**(`categoryOf`). 명세가 따로 들면 생체신호 그룹을 담은
    '기본정보 명세' 같은 어긋난 조합이 생긴다
  - 이름 겹침은 막지 않고 알린다 — 웹도 기관마다 같은 이름의 `기본명세`를 둔다
  - ⚠️ 어떤 필드가 담기는지는 웹 화면에 없어 그룹의 필드를 그대로 담았다(임의값).
    버전·작성자는 앱이 읽는 곳이 없어 받지 않는다

- **프로그램 저작**(`/authoring/program/:id`, `ProgramEditorView.vue`). 편집 화면 5호
  - **회차의 이름과 순서, 무엇을 붙일지까지가 이 화면의 몫이다.** 회차 안의 내용은
    세션 활동이 만들고 여기서는 **고른다**(데이터 명세가 필드를 고르는 것과 같은 자리)
  - **회차 하나 = 이름 + 붙인 활동**(`activityId`). 붙으면 세션 상세가 그 활동의
    치유단계를 읽고, 붙지 않은 회차만 `phasesOf`의 생성기가 골격으로 채운다
  - **고를 수 있는 것은 `작성 완료`된 같은 증상의 활동뿐이다.** 증상을 바꾸면
    붙일 수 없게 된 활동은 떨어진다(프로세스 저작이 설문을 떨어뜨리는 것과 같다)
  - **효과성(rating)은 받지 않는다** — 수행 기록에서 나오는 값이다. 새 프로그램은 `-`이고
    0으로 두지 않는다('효과가 없다'는 측정 결과로 읽힌다)
  - 목업의 `orgPlace`를 `org` + `place`로 나눴다. 한 문자열이면 기관 이름이 두 자리에 적힌다
  - `programs`도 reactive다(저작이 고치면 목록·처방 화면이 함께 갱신)
- **저작 즐겨찾기 · 홈의 빠른 저작**(`mocks/favorites.js` · `FavoriteToggle.vue`).
  **단위는 도구다** — 일곱 중에서 고른다. 도구 화면(`AuthoringToolView`) 머리 우측의
  별표로 담고 홈이 편다. 칸을 누르면 그 도구로 간다
  - **이름을 저장하지 않는다.** 도구 키만 남기고 이름·아이콘은 그릴 때 정의에서 읽는다
  - 칸의 모습(아이콘 24 + 이름 한 줄, 2열)은 그대로다. 바뀐 것은 **무엇이 오는가를
    쓰는 사람이 정한다**는 것뿐이다. 별표에 accent를 쓰지 않는다(용법 셋 밖이다)
  - 머리(`빠른 저작` + chevron) 전체가 저작도구 홈으로 가는 길이다
  - **넷까지다.** 일곱을 다 담으면 칸이 네 줄이 되어(336 vs 196) 작업 목록이 밀려난다.
    다섯 번째 별을 누르면 **사유 콜아웃**이 뜬다(무반응 금지). 빼는 것은 언제나 된다
  - ⚠️ 계정에 붙을 값인데 지금은 메모리다 — 새로고침하면 기본값 넷으로 돌아간다
- **감정평가 분류의 데이터 그룹(EA)** — **설문 코드별 총점**을 필드로 본다.
  앱이 읽는 값이 총점 하나뿐이고, 문항 단위로 두면 척도를 고칠 때마다 필드 수가 흔들린다.
  필드는 `surveys`에서 만들고 앞에 `평가 시점`·`평가일자`를 둔다. ⚠️ 임의 결정이다

- **세션 활동 저작**(`/authoring/activity/:id`, `ActivityEditorView.vue`). 편집 화면 6호.
  구버전 웹 화면을 옮겼다 — 좌 메타데이터 / 우 블록 구성
  - 활동 하나 = 치유단계 여럿, 단계 = 블록 여럿, 블록 = **활동 종류 하나 + 내용 하나**.
    블록은 `안내문`과 `인문 문장` 둘로 갈리고 **안내문은 적고 인문 문장은 고른다**
  - **인문 문장은 도서 콘텐츠를 가리킨다**(`bookId` + `sentence` 번호). 문장을 옮겨
    적으면 같은 문장이 자리마다 다시 적히고 어느 책 몇 쪽에서 왔는지도 남지 않는다.
    도서를 바꾸면 고른 문장은 비운다(번호는 그 문단 안에서만 뜻이 있다)
  - 무엇이 채워진 블록인가는 종류가 정한다(`blockFilled`)
  - **좌우로 나눈 것은 이 일의 사정이다** — 블록 문구를 쓰는 동안 대상·소요시간이
    사라지면 안 된다(처방 화면의 탭 결정과 같다)
  - **`작성 완료`가 배치의 자격이다.** 덜 채워진 활동이 회차에 붙으면 환자를 만나는
    자리에 빈 안내문이 오른다
  - **인문 활동 종류는 받지 않는다** — 블록마다 이미 고른다. 치유 구성요소 · 주제어 ·
    증상 강도 · 연령대도 읽는 화면이 없어 뺐다. **치유대상 감정만** 남겼다(세션 상세가 읽는다)
  - 머리의 `초기화`·`미리보기`는 두지 않았다(초기화는 이탈 경고가, 미리보기는 볼 화면이 없다)
  - `작성 완료(세션에 배치 가능)`는 남겼다. 아직 아닌 것에 `작성 중` 배지가 붙는다
  - 저장할 때 **빈 블록과 블록이 없는 단계는 걷는다.** 삭제는 확인을 거치고 빈 것만 바로 지운다
  - **필요 도구는 자유 입력이다.** 웹은 고정 체크박스 여덟이지만 붙은 활동의 준비물이
    세션 상세에 그대로 오르므로 프로그램의 준비물과 같은 자를 써야 한다
  - ⚠️ 치유단계 유형은 `도입`·`투사`만 웹 실측이고 나머지는 임의다. `programs.js`의
    PHASE 이름과 같은 축인지도 미확인 — 배치가 붙으면 이것이 PHASE 자리에 들어간다

- **도서 콘텐츠 저작**(`/authoring/book/:id`, `BookEditorView.vue`). 편집 화면 7호(마지막).
  구버전 웹 `문장 명세`를 옮겼다 — 좌 기본 정보 / 우 추출 문장 속성값
  - **한 건 = 한 도서에서 뽑은 문단 하나다.** 서지 정보 + 추출 페이지·문단 + 속성값
  - **여기서는 필드를 줄이지 않았다** — 왼쪽이 전부 작품을 특정하는 서지 정보라
    하나만 빠져도 어느 책 몇 쪽인지 가리키지 못한다. 뺀 것은 `DCAT 데이터셋` 하나다
    (입력이 아니라 내보내는 자리라 붙일 곳이 없다)
  - **속성 분류는 화면이 들지 않는다**(`BOOK_ATTRIBUTES`). 축 다섯 · 소분류 열둘 ·
    항목 45를 목업의 배열에서 그린다 — 가장 먼저 바뀔 값이라 한 곳에서 끝나야 한다.
    머리의 `45개 항목`도 세어서 낸다
  - 축은 접힌다(머리와 본문이 한 상자). 소분류가 없는 축은 이름 줄을 두지 않는다
  - 저장할 때 **분류에서 사라진 값은 걷는다**
  - ⚠️ 같은 도서에서 문단을 여럿 뽑으면 서지 정보가 건마다 되풀이된다.
    도서 하나 아래 발췌 여럿이 옳지만 백엔드 단위가 미확정이라 웹을 따랐다
  - **문장은 문단에서 끊어 낸다. 따로 저장하지 않는다**(`sentencesOf`).
    끊는 규칙은 마침표·물음표·느낌표 다음의 공백이고, 번호가 범위를 넘으면
    `가리킨 문장을 찾을 수 없습니다`라고 말한다 — 없는 문장을 지어내지 않는다
  - **문단을 고치면 저장하기 전에 알린다**(`citationsOf`). 문장이 늘거나 줄면 뒤쪽
    번호가 밀려 다른 문장을 가리키게 된다 — 막지 않고, 어느 활동의 어느 단계가
    몇 번째 문장을 가리키는지 목록으로 보여준다. **문단을 고쳤을 때만** 묻는다
  - ⚠️ 문장을 끊는 규칙은 임의다(마침표·물음표·느낌표 다음의 공백)

**저작도구 일곱 도구의 편집 화면이 모두 열렸다.**

- **자주 바뀔 값은 화면이 아니라 목업의 배열에 둔다.** 백엔드·팀 방향에 따라
  크게 바뀔 영역이라 구성이 한 곳에서 끝나야 한다 — 도서 속성 45(`BOOK_ATTRIBUTES`) ·
  활동 종류(`ACTIVITY_KINDS`) · 치유단계 유형(`PHASE_TYPES`) · 필드 유형(`FIELD_TYPES`) ·
  분류(`FIELD_CATEGORIES`) · 운영형태(`PROGRAM_FORMS`)가 그 예다
- **축소 원칙은 현행 유지로 확정됐다**(설계문서 6.2절 46번). 여섯 화면은 앱이 읽는
  필드만 받고, 도서 콘텐츠만 웹의 필드를 그대로 받는다(왼쪽이 전부 서지 정보다).
  백엔드가 다른 필드를 들고 오면 그때 한 줄씩 넓힌다 — **유연성은 필드의 개수가
  아니라 자주 바뀌는 값을 목업의 배열에 두는 것으로 확보한다**

**메모는 `src/mocks/notes.js`의 `reactive` 배열 하나에서만 온다.** 환자 상세
화면에서만 쓰이므로 공유 상태 둘(`dragState`·`scheduleState`)에 합류시키지 않았다.
모듈이 싱글턴이라 화면을 나갔다 와도 편집 결과가 남는다.

**배치 되돌리기는 스낵바 실행취소가 아니라 일정 편집·삭제 경로**다.
시간 제한이 있는 실행취소는 "저장은 명시적 조작으로만" 원칙과 어긋난다.

- **편집은 새로 만드는 게 아니라 옮기는 것이다.** 대상(누구와 만나는가)은 안 바꾼다 —
  대상을 바꾸는 건 지우고 새로 잡는 일이다. 날짜·시간·유형만 고른다
- **편집 폼은 `ScheduleEntryModal`의 `mode="edit"`다.** 폼을 따로 만들면
  시간 선택 규칙과 중복 경고가 한쪽에만 남는다
- **편집 중인 일정은 자기 자신을 막지 않는다.** `openHoursOn`·`duplicateOn`·
  `dateChoices` 셋 다 `exceptEventId`를 받는다. 자기가 점유한 시간·날짜가
  후보에서 빠지면 '날짜는 그대로 두고 시간만 옮기기'가 아예 불가능해진다
- **환자 일정에는 id가 있다**(`ev-N`). 이름+시간으로 찾으면 같은 날 같은 환자를
  두 번 만나는 경우에 갈린다. 업무 블록은 작업에서 파생하므로 작업 id를 쓴다

공유 상태는 세 개다. 늘리기 전에 재검토할 것.
- `src/authState.js` — 로그인 세션. 목업이 아니라 **앱의 상태**라 mocks에 두지 않았다.
  라우터 가드가 매 이동마다 읽고 셸을 그릴지도 여기서 갈린다
- `src/dragState.js` — 꾹 누르기·드래그. 배치로 끝난 제스처의 click을 삼키는
  `swallowDragClick`도 여기 있다(App.vue가 캡처 단계에 건다).
  **손을 뗀 자리가 누른 자리와 다르면 click이 아예 오지 않는다.** 그때 플래그가 남아
  다음 탭을 삼키므로 `beginGesture`가 pointerdown마다 푼다 — 뺄 수 없는 짝이다
- `src/scheduleState.js` — **환자 일정과 작업의 유일한 가변 상태.** 화면이 사본을 들면
  아젠다에서 배치한 일정이 캘린더에 보이지 않는다.
  `eventsOn(key)`가 환자 일정과 **작업에서 파생한 업무 블록**을 합쳐 돌려준다

`src/components/ProcessDetailModal.vue`(프로세스 상세·할당)와 `ProcessStartStep.vue`(0단계 본문)가 프로세스 시작을 맡는다.
`ModalShell`에 690×480 `variant="large"`를 더했다 — 무거운 콘텐츠용이고 history 처리는 그대로 공유한다.

`src/components/ScheduleEntryModal.vue`가 배치 확인·일정 추가·작업 추가를 모두 담당한다.
중복 경고와 배치 규칙이 이 안에만 있어야 화면별 우회 경로가 생기지 않는다.
`src/components/ModalShell.vue`가 셸과 history를, `TaskDetailModal.vue`가 작업 상세를 맡는다.

## 다음 세션에서 할 일

### ~~1. pressed 피드백 마무리~~ — 완료 (2026-08-14)

지난 행 통일 · 캘린더 인접 달 칸 `active:opacity-100` · 환자 상세(탭 · 히스토리 행 ·
Key data 카드) · 전체 환자 리스트(필터/정렬 버튼 · 칩 · 두 팝오버 행) 모두 적용하고
DOM 실측으로 확인했다. 스테퍼 노드는 탭 대상이 아니라 걸지 않았다.

**남은 것 하나** — 선택된 탭에는 pressed가 없다. 텍스트만 있는 버튼은
`active:text-text-primary`로 눌리는데, 선택된 탭은 이미 `text-interactive-default`라
글자색을 올리면 accent를 잃는다. 재선택이 무동작이라 일단 비워뒀다. 판단 필요.

### ~~2. 개인 메모 탭~~ — 완료 (2026-08-14)

목록(`188:5004`) · 편집(`189:8675`) · 삭제 확인(`190:8960`) 세 상태 모두 구현.
미저장 이탈 3버튼 경고(`186:6839`)도 함께 만들었다. DOM 실측으로 확인했다.

- **새 메모도 같은 편집 카드다.** 목록 맨 앞에 임시 카드를 끼워 같은 `v-for`가 처리한다.
  마크업을 두 벌 두면 한쪽만 고쳐진다
- **편집 중 카드는 테두리가 accent**로 바뀐다(선택 상태는 배경만으로 표현하지 않는다)
- **맥락 태그는 고르는 것이 아니라 환자의 현재 단계를 따른다.** 배치 단계 결정과 같은 이유다
- **미저장 경고는 `guard()` 한 곳으로 모은다** — 탭 전환 · 다른 메모 편집 · 새 메모 ·
  삭제 · 화면 이탈(버튼 + `onBeforeRouteLeave`). 흩어놓으면 한 경로만 빠져나간다
- **Figma와 의도적으로 다르게 둔 것 셋**:
  본문 `line-clamp-2`(Figma 한 줄 — 뒷부분을 읽으려고 편집을 열면 미저장 경고까지 끌어온다) ·
  본문 `text-label`·태그 `text-caption`으로 한 단계씩 상향(11이면 카드에서 제일 중요한
  본문이 화면에서 제일 작다. 날짜>본문>태그 위계는 유지) ·
  카드 패딩 `px-3` 대칭 + 아이콘 묶음만 `-mr-2`(Figma의 `pl-12 pr-4`는 아이콘 히트
  영역이 만드는 여백을 감안한 값이라, 그대로 두면 본문·입력폼의 좌우가 갈린다)
  - `onBeforeRouteLeave`는 `to.path === from.path`면 통과시킨다.
    모달이 쿼리를 붙이며 일으키는 이동까지 막으면 경고 모달 자신이 열리지 못한다
- ⚠️ Figma 확정 버튼 라벨 건은 **해소됐다 — `삭제`가 맞다.** 레이어 *이름*만 다른 화면
  인스턴스의 것(`프로그램 처방으로 이동`)이 남아 있었고, 실제 문자열은 두 글자다

### ~~3. 일정 편집·삭제~~ — 완료 (2026-08-14)

### ~~4. 비활성 사유 콜아웃~~ — 완료 (2026-08-18)

### ~~5. 코어 프로세스 6단계~~ — 0~5단계 모두 완료 (2026-08-19)

**공통 셸은 만들었다** (`views/ProcessView.vue`, 라우트 `/process/:id/:step`).
환자 정보 + 컴팩트 스테퍼 + `환자 상세` 버튼. 우측 패널 없음(본문 929 실측).
**스테퍼 연결선은 `flex-1`이다** — 고정 폭으로 두면 스테퍼의 최소 폭이 본문을 밀어내
중앙 패널 929를 넘긴다. 루트의 `min-w-0`도 같은 짝이다.

| 단계 | Figma | 상태 |
|---|---|---|
| 0 프로세스 시작 | `148:7242` + `148:7729` `173:5511` `172:4483` `173:5793` | **구현 완료** |
| 1 감정평가(사전) | `148:7935` (`176:5079`는 콜아웃) | **구현 완료** |
| 2 프로그램 처방 | `148:8145` `178:3788` `179:4283` `180:4914` | **구현 완료** |
| 3 프로그램 수행 | `148:8311` | **구현 완료** |
| 4 감정평가(사후) | 사전과 같은 화면 | **구현 완료** |
| 5 프로세스 종료 | `148:8564` (본문 `148:8601`) · 사후 하단 `186:6607` | **구현 완료** |

⚠️ 이전 인수인계가 `148:7729` `173:5511` `172:4483` `173:5793`을 '단계 1~4'로
적어 두었으나 **틀렸다.** 넷 다 프로세스 시작 화면의 **상세 모달 상태**다
(진단 일치·불일치 × 훑어보기·확인). 나머지 네 단계의 Figma 노드는 아직 모른다.

#### 프로세스 시작(0단계) 확정 사항

- **진단 칩은 둘뿐이다** — 환자의 진단(기본) / 전체. 라이브러리를 훑는 화면이 아니라
  이 환자에게 붙일 것을 고르는 화면이라 기본 상태가 곧 맞는 답이어야 한다
- **진단이 다른 프로세스도 고를 수 있다.** 막지 않고 알린다 — 칩 · 구성 머리 ·
  확인 문구 **세 자리**가 경고색이고 확정 버튼도 `할당 확정` → `그래도 할당`으로 바뀐다
- **할당은 확인을 거친다.** 목록에서 바로 확정하는 길은 없다.
  `할당하기`를 누르면 **버튼 행만 확인 상태로 바뀌고**, `취소`는 모달을 닫지 않고
  훑어보던 자리로 돌아간다(중복 경고의 `돌아가기`와 같은 문법)
- **단계 아코디언은 머리와 본문이 한 상자다.** Figma는 두 상자로 두었으나
  '펼쳐지는 목록' 규칙을 따랐다 — 의도적 차이다
- **모달 높이 480 고정.** 아코디언을 여닫을 때 버튼 행이 움직이면 안 된다
- 할당이 끝나면 환자의 `process`·`status`·`nextStep`이 바뀌고 **1단계로 넘어간다**
- **이력도 함께 바뀐다** — 돌아가던 프로세스가 `종료`로 닫히고(종료일 + 사유)
  새 프로세스가 `진행 중`으로 열린다. `processHistory` 목업은 40명이 함께 보므로
  **직접 고치지 말 것.** 환자별 변경분만 쌓고 `historyOf(patient)`가 읽을 때 합친다
- ⚠️ 이미 할당된 환자의 재할당 처리는 Figma에 없다. 지금은 할당된 행을
  선택 상태로 표시하고 재할당을 허용한다 — 확인 필요
- ⚠️ 정렬 선택지와 '프로그램 처방·수행' 단계를 펼쳤을 때의 내용은 초안이다

#### 감정평가(1·4단계) 확정 사항

- **사전과 사후가 같은 화면이다.** 시점은 단계 번호가 정한다(1=사전, 4=사후).
  화면 안의 칩으로 오갈 수 있지만 **도달하지 않은 시점은 못 연다** —
  막힌 칩을 누르면 비활성 사유 콜아웃이 뜬다(스테퍼 노드와 같은 규칙)
- **설문 목록은 프로세스가 정한다.** 화면이 목록을 따로 들지 않고 할당된
  프로세스의 감정평가 단계 구성을 그대로 읽는다
- 배지 둘: **핵심/보조**(판정 근거인가 참고인가)와 **진단/공통**.
  핵심만 채운 배지다
- 점수는 **막대 + 컷오프 마커**를 함께 보여준다. 점수 하나로는 높은지 낮은지 모른다.
  미작성은 값 자리가 `-`이고 내용이 흐려진다 — 없는 값을 있는 것처럼 그리지 않는다
- **작성완료는 그 단계에 있는 동안만 다시 열 수 있다.** 막으면 잘못 찍은 응답을
  되돌릴 길이 없지만, **다음 단계로 넘어가면 잠긴다** — 뒤 단계가 이미 참조한
  점수(처방 등급·감정 지표)와 어긋난다. 지난 단계에서 누르면 사유 콜아웃이 뜨고
  다음 단계 버튼도 함께 잠긴다 (`locked = step < stepIndexOf(patient)`)
- 다음 단계 버튼은 **모든 설문이 작성되어야 활성**이고, 라벨은
  **다음 단계 이름을 따른다**(사전→`프로그램 처방으로 이동`, 사후→`프로세스 종료로 이동`)
- ⚠️ 출력 버튼은 동작하지 않는다. 문서 양식 미확정 — 사유 콜아웃만 띄운다

#### 프로그램 처방(2단계) 확정 사항

- **한 화면에 탭 둘**: `감정평가 정보` / `프로그램 선택`. 화면을 나누면 고르는
  도중 근거를 다시 보려고 화면을 나가야 한다. 세그먼트 토글은 좌측 하단 테마
  토글과 같은 문법이다
- **감정평가 결과는 다시 계산하지 않는다.** 1단계가 남긴 응답을 읽는다 —
  두 화면이 다른 점수를 보이면 어느 쪽이 맞는지 알 수 없다
- 등급 배지 넷: `고위험` / `중증도` / `정상` / `미작성`. **고위험만 경고색**이다.
  **값이 없는 것을 `정상`으로 두지 말 것** — 안 한 것과 괜찮은 것이 같아 보인다
- ⚠️ 등급 판정은 **Figma 4건에서 역산한 규칙**이다(컷오프 초과 + 핵심/보조).
  `surveys.js`의 `gradeOf` 하나에 있다 — 실제 기준은 척도마다 다르므로 확인 필요
- 감정 지표는 다섯 축 세로 막대다. `0–100 정규화` · **높을수록 양호**를 머리에
  밝힌다. 방향을 적지 않으면 높은 막대가 좋은지 나쁜지 알 수 없다.
  ⚠️ 값은 Figma 실측 임시값이고 설문에서 뽑는 규칙은 미확인이다
- **목록에는 이 환자의 진단에 맞는 프로그램만 오른다.** 프로세스 할당과 달리
  다른 진단을 고르는 길을 두지 않았다 — 프로그램은 이미 진단이 정해진 뒤의 내용물이다
- 고른 행에는 **체크가 붙는다**(배경·테두리만으로는 잘 안 보인다). 하단에 무엇을
  골랐는지 한 줄로 남기고, 고르기 전에는 다음 단계 버튼이 비활성 + 사유 콜아웃
- 필터 팝오버 첫 줄은 언제나 `전체`다. 푸는 길이 없으면 막다른 길이 된다
- **프로그램 상세와 세션 상세는 한 모달이다.** 세션을 보는 동안 머리의 X가
  chevron-left로 바뀐다. 모달을 겹치면 history가 두 겹이 되어 뒤로가기를 두 번
  눌러야 목록으로 돌아온다
- 세션 안에서는 **PHASE를 페이지로 넘긴다**(`1/5`). 마지막에서는 `세션 목록`으로 바뀐다

**`src/mocks/programs.js`가 프로그램 정의의 유일한 출처다.** 회차는 이름과 붙인
활동만 들고, **회차 안의 PHASE·활동 문구는 `activities.js`가 원본이다.**
지어내던 생성기는 걷었다 — 활동이 붙지 않은 회차는 비어 있고 세션 상세와
프로그램 수행 화면이 `이 회차에 붙은 세션 활동이 없습니다`라고 말한다.
Figma 문구가 있던 한 단계는 활동 정의(`act-4`)로 옮겼다.
⚠️ Figma에 실제로 적힌 것은 프로그램 4건의 요약 행, `마음챙김 호흡 이완 훈련`의
세션 6개 이름, 1세션 PHASE 1의 활동 문구뿐이다. **나머지 세션·PHASE는 생성기가
같은 골격으로 채운 임의값**이다(`phasesOf`).

#### 프로그램 수행(3단계) 확정 사항

- 좌 회차 목록 + 우 회차 상세를 **한 화면**에 둔다. 기록하는 도중 몇 회차인지
  다시 보려고 화면을 나가면 안 된다 — 처방 화면의 탭 결정과 같은 이유다
- **선택은 행의 accent 테두리가, 진행 상태는 오른쪽 배지가 말한다.** 대개 같은
  행이지만 지난 회차의 기록을 볼 때 갈린다. accent 용법이 겹치지 않게 나눈 것이다
- 상태 배지 셋: 완료(체크+중립) / 진행 중(accent 채움) / 대기(흐린 표면)
- 아직 오지 않은 회차는 **기록할 것이 없다.** 입력이 비활성이고 누르면 사유 콜아웃
- 주 행동은 `저장 후 N회차로`다. **저장과 회차 완료를 한 조작으로 묶는다** —
  마쳤다는 사실과 그때의 기록이 같은 조작에서 나와야 한다.
  마지막 회차에서는 `저장 후 감정평가로`가 되고 다음 단계로 넘어간다
- 회차의 **이름·개수는 처방된 프로그램이 정한다.** 화면이 목록을 따로 들지 않는다
- **회차 날짜는 일정에서 온다**(`visitDatesOf`). 끝낸 회차는 지난 진료일을 뒤에서부터,
  남은 회차는 앞으로 잡힌 진료일을 앞에서부터 가져간다. 오늘 잡힌 일정은 시각이 지났어도
  '남은 것'이다 — 지금 하고 있는 회차다.
  **약속이 모자라면 `일정 미정`**이고 날짜를 지어내지 않는다.
  회차에 날짜를 따로 저장하면 일정과 두 곳에 같은 사실이 남아 한쪽만 갱신된다

**`src/mocks/sessions.js`가 회차 진행 상태의 유일한 출처다.**
프로그램 정의(`programs.js`)는 아직 아무에게도 붙지 않은 내용이고,
여기는 붙은 뒤의 기록이다 — `processLibrary.js`와 `process.js`의 관계와 같다.

#### 프로세스 종료(5단계) 확정 사항

- **결정하는 화면이 아니라 결과를 보여주는 화면이다.** 종결 조작은 앞 단계
  (감정평가 사후)의 **`종료 확정`**이 이미 했다 — 그래서 하단에 다음 단계 CTA가 없다
- **사후 감정평가의 주 버튼만 다르다**: 라벨이 `종료 확정`이고 왼쪽에
  `<이름> 환자의 프로세스를 종료하시겠습니까? 현재 시각: …`을 둔다(Figma `186:6607`).
  '이동'이라고 적으면 되돌릴 수 있는 화면 전환으로 읽힌다
- **종결은 `completeProcess` 한 곳이다.** 프로세스 상태·현재 단계·종료 시각이 함께
  바뀌고 이력의 진행 중 항목이 닫힌다. 화면이 환자 레코드를 직접 고치면 이력이 안 닫힌다
- **이미 끝난 프로세스에는 종료를 묻지 않는다.** 완료 환자의 사후 화면은 스테퍼로
  다시 열리는데, 그때도 확인 문구가 뜨면 안 끝난 것으로 읽힌다. 문구는 종료 사실로
  바뀌고 버튼은 `프로세스 종료 보기`가 된다(잠긴 채 못 가는 버튼을 남기지 않는다).
  `complete()`도 완료 환자를 다시 종결하지 않는다 — 종료 시각이 오늘로 덮인다
- **종료 시각은 끝내는 순간에 정해진 사실이다.** 화면이 열릴 때 지금 시각을 찍으면
  볼 때마다 값이 달라진다. 목업의 완료 환자도 씨앗이 지난 날짜로 채운다
- 사전·사후는 **같은 트랙 위 두 막대**다(사전 흐림 / 사후 진함). 한쪽이 없으면
  막대를 그리지 않고 `비교할 수 없음`이다
- **악화만 경고색**이다. 개선·변화 없음은 중립색
- ⚠️ **점수의 방향이 Figma 안에서 갈린다** — 종료 화면은 `높을수록 양호`인데
  처방 화면의 `gradeOf`는 컷오프 초과를 고위험으로 본다. 앱이 이미 쓰는
  **`낮을수록 양호`**를 따랐다. 확정되면 설문 정의가 방향을 들고 오게 할 것
- 목업의 **사후 응답은 사전에서 한 단계씩 내려간 값**이다(`fillPostFromPre`).
  시점마다 따로 뽑으면 사전과 점수가 같은 환자가 생겨 비교가 '변화 없음'만 남는다

#### 설문 수행 화면 (Figma 없음 — 임의 제작)

- **셸 전체를 걷는다.** 라우트 `meta.bare`가 `App.vue`에서 셸을 통째로 건너뛴다
- **뒤로가기 차단은 `onBeforeRouteLeave` 한 자리다.** `history.pushState`로 직접
  엔트리를 만들지 말 것 — 라우터가 모르는 엔트리가 끼면 모달의 `router.back()`이
  낸 popstate까지 이 화면이 삼켜 경고가 닫히지 않는다(그렇게 만들었다가 고쳤다)
- **이 화면의 경고만 `ModalShell`을 쓰지 않는다.** 경고 자체가 뒤로가기에 대한
  응답이라 history 엔트리를 가지면 *경고를 닫는 back*과 *화면을 나가려는 back*이
  같은 제스처를 두고 다툰다. 상자 모양은 alert과 같게 두고 엔트리만 뺐다
- 보기 하나가 통째로 터치 대상이다(88×44). 원만 누르게 하면 44 하한에 못 미친다
- 제출은 **모든 문항에 응답해야** 가능하고, 미완 제출을 누르면 몇 문항이 남았는지 말한다
- **중간에 멈춘 설문에도 나갈 길이 있다.** 뒤로가기가 3버튼 경고로 갈라진다 —
  `저장하지 않고 나가기` / `임시 저장하고 나가기` / `계속 응답`.
  화면에 상시 나가기 버튼을 두면 환자가 무심코 누른다
- 임시 저장은 **저장했다는 사실을 남기는** 조작이다. 그 사실이 있어야 감정평가
  목록이 `이어서 작성 N/M`을 보여주고 경고 문구도 달라진다
- 제출하면 **완료 알림 화면**으로 바뀐다. 셸은 여전히 걷은 채고, 복귀 버튼은
  화면 아래에 따로 떨어뜨린다 — 완료 문구에 붙이면 환자가 확인 버튼으로 읽는다

**`src/mocks/surveys.js`가 설문 정의와 응답의 유일한 출처다.**
`processLibrary.js`는 코드만 들고 이름을 여기서 가져간다 — 두 곳에 적으면
프로세스 상세 모달과 감정평가 화면의 설문 이름이 갈라진다.

- ⚠️ **문항·설명·척도는 전부 임의로 지은 목업이다.** 척도 이름과 문항 수는
  실제 도구를 흉내 냈지만 문장은 원문이 아니다. 실제 문항은 저작권과 표준화
  절차가 걸린 자료다 — 확정되면 이 파일을 통째로 갈아끼운다
- 응답은 `환자:시점:코드` 키의 `reactive` 객체다. 메모와 같은 자리로,
  공유 상태 둘(`dragState`·`scheduleState`)에 합류시키지 않았다
- ⚠️ 기존 환자는 `processId`가 없어 `processFor(patient)`가 진단에 맞는 최신
  프로세스로 떨어뜨린다. 임시값이다 — 환자 레코드가 프로세스 id를 들고 오면 걷어낼 것

**`src/mocks/processLibrary.js`가 프로세스 정의의 유일한 출처다.**
`mocks/process.js`(진행 중인 프로세스의 이력·지표)와는 다른 데이터다 —
전자는 아직 아무에게도 붙지 않은 정의, 후자는 붙은 뒤의 기록이다.

**`src/mocks/seed.js`가 명단 40명에 프로세스·감정평가 응답·처방을 심는다.**
`main.js`에서 한 번 부른다. 명단에는 이름·진단·단계밖에 없어 화면이 전부 비어 보였다.

- **값은 결정론적이다** — 환자 id를 FNV-1a로 해시해 씨앗으로 쓴다.
  `Math.random`을 쓰면 새로고침마다 점수가 달라져 실측 검증이 불가능하다
- **채우는 범위는 현재 단계가 정한다.** 지나온 단계만 값이 있고, 지금 단계는
  일부만(작성 중) 있고, 아직 오지 않은 단계는 손대지 않는다.
  시작 전 환자는 프로세스를 붙이지 않는다 — 0단계 화면이 할 일을 잃는다
- 씨앗을 시프트할 때 `>>`를 쓰면 음수가 되어 인덱스가 빗나간다. `>>>`를 쓸 것
- 실제 데이터가 오면 이 파일만 걷어내면 된다

**코어 프로세스의 단계는 프로세스 정의가 정한다**(`stepsOf`). 노드 수도 순서도
정의마다 다르므로 화면이 단계 목록을 상수로 들지 않는다. **프로세스 시작·종료만
고정된 양끝**이고 저작에서 다루지 않는다. **조건 분기는 없다**(확인 완료).

- 단계 본문은 **번호가 아니라 노드의 종류**로 갈린다(`node.type`)
- 감정평가의 시점(사전·사후)도 노드가 들고 있다. `종료 확정`은 사후 노드에서만 나온다
- **현재 단계의 원본은 환자 레코드의 `stepIndex`(단계 번호)다.** `status`·`nextStep`
  문자열은 레코드에서 걷었고 `statusOf`·`nextStepOf`가 정의에서 읽어 만든다.
  문자열을 대조해 번호를 역산하던 시절에는 같은 종류의 노드가 둘인 정의에서
  **언제나 앞의 것이 잡혀** 뒤 노드에 도달할 수 없었다
  - `status`는 표시용으로 남는다 — 우측 패널·리스트 열·`현재 단계` 축·배치 모달이
    모두 `statusOf`를 부른다. 읽는 곳은 그대로 두고 **드는 곳만** 없앴다
  - 프로세스가 붙지 않은 환자는 `접수 완료`다. 0단계 이름을 그대로 보이면 이미 시작한 것으로 읽힌다
  - 정의가 바뀌어 단계 수가 줄면 번호를 끝 단계로 가둔다

**환자 명단(`mocks/patients.js`)은 `reactive`다.** 할당이 환자 레코드를 고치는데
평범한 배열이면 스테퍼도 우측 패널도 분석 화면도 그 자리에서 갱신되지 않는다.

- 스테퍼는 **환자 상세와 코어 프로세스 양쪽에 있고 판정 규칙이 같아야 한다.**
  판정은 `mocks/process.js`의 `stepStateOf` 하나로 올렸다 — 복제해 뒀을 때
  '시작 전 환자가 어느 단계도 열 수 없는' 결함이 양쪽에 똑같이 생겼다
- 설문 **수행** 화면은 셸 전체를 걷는다(위 '설문 수행 화면의 확정 사항')

그 다음은 상세 치유 프로세스 목업이 나오면 `mocks/process.js` 임시값 교체.

### 설문 수행 화면의 확정 사항 (미착수 화면이지만 미리 정해둔 것)

- **이탈 차단은 UI 경로 제거까지다.** 기기 화면 고정(Screen Pinning)은 전제하지 않는다 —
  앱이 제어할 수 없는 시스템 설정에 안전성을 걸 수 없다
- **뒤로가기가 오면 경고 모달**을 띄운다. 무반응은 고장으로 읽힌다
- **환자가 종료하는 유일한 경로는 제출 버튼**이고, 제출되면 **완료 알림 전용 화면**으로
  넘어간다. 이 화면도 셸을 걷은 채다 — 태블릿이 아직 환자 손에 있다.
  상담사용 복귀는 이 화면의 명시적 조작으로만 한다
  - ⚠️ 복귀 조작의 형태(버튼·위치·라벨)는 미정. 환자가 무심코 누를 자리는 피할 것

Figma는 `.mcp.json`의 원격 서버(`https://mcp.figma.com/mcp`)로 읽는다.
인증은 대화형 터미널에서 `claude.cmd` → `/mcp` → figma로 한 번 해두면 유지된다
(PowerShell 실행 정책 때문에 `claude`가 아니라 `claude.cmd`로 불러야 한다).
붙어 있지 않으면 노드 값을 확인할 방법이 없으니 **추측해서 채우지 말고 먼저 알릴 것.**
`get_design_context`를 부르기 전에 `figma-design-to-code` 스킬을 먼저 읽어야 한다.

### 검증 방법

`preview_start`로 dev 서버를 띄우고 뷰포트를 1138×712로 맞춘 뒤,
브라우저에서 **DOM을 직접 측정**해 Figma 값과 대조해 왔다. 스크린샷만으로는
1~2px 차이나 토큰 오적용이 드러나지 않는다. 드래그는 `PointerEvent`를 직접
dispatch해 검증한다(`pointerdown` → 500ms 대기 → `pointermove` → `pointerup`).

⚠️ 캘린더 검증 시 주의: `startsWith('30')`으로 칸을 찾으면 **6월 30일**(흐린 칸)이
먼저 잡힌다. 흐리지 않은 칸(`opacity === '1'`)만 걸러서 찾을 것.

## 아직 안 연 Figma 참고 노드

환자 상세 · 코어 프로세스 작업을 시작할 때 열 것 (파일 키 `pyY0dZcLU0QtHsqwh3DOSk`)

- 환자 화면은 모두 열어 구현했다: `87:763`(분석, 본문 `127:9495`) ·
  `127:8671`(전체 리스트, 본문 `127:8692`) · `130:2792`(리스트 빈 상태) ·
  `130:3152`(상세 — 프로세스 히스토리) · `148:5384`(상세 — 핵심 지표) ·
  `188:5004`·`189:8675`·`190:8960`(개인 메모 3상태)
- 미저장 이탈 3버튼 경고도 열어 구현했다 (`186:7141` 안의 alert이 `186:6839`)
- 비활성 사유 콜아웃도 구현했다 (`176:5079` 안의 콜아웃이 `176:5336` · `176:5225`)
- 프로세스 시작도 다 열어 구현했다: `148:7242`(리스트) · `148:7729`(상세 모달) ·
  `173:5511`(할당 확인) · `172:4483`(진단 불일치) · `173:5793`(불일치 확인).
  모달 본체는 `172:2899`, 불일치 머리·배너는 `172:4556`·`172:4572`다
- `189:8259` `188:7591` — calendar modal / row 컴포넌트

**모달 history는 반드시 라우터를 거친다.** 열 때 `router.push({ query: { modal } })`,
닫을 때 `router.back()`, `route.query.modal`이 사라지면 닫는다.
`history.pushState`로 직접 엔트리를 만들면 vue-router가 모르는 엔트리가 생기고,
`history.back()`의 popstate를 라우터가 route 이동으로 처리해 다른 화면으로 튕긴다.

미해결:
- 실기기 논리 해상도 실측 (기기 미수령). Figma 프레임 1138×712도 추정값
  - 712에는 상태바 + 제스처바 61이 포함되어 있고, Figma 셸 콘텐츠는 603
  - PWA standalone 뷰포트는 시스템 UI를 제외하므로 `100dvh` ≈ 651 → `p-6` 제하면 603이 자연히 나온다
  - **따라서 603을 하드코딩하지 말 것.** 실기기에서 `innerHeight`가 651 근처인지만 확인하면 확정
- `interactive/pressed`가 다크모드에서 `default`와 동일 (brand/300) → pressed 피드백 소실
- `interactive/primary-fill`(neutral/900) vs 다크 `surface/canvas`(neutral/950) 명도 차 부족
- 효과성 분석 · 저작도구 화면은 Figma 디자인 자체가 미완성
